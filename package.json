{
	"name": "simvyn-monorepo",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/types",
		"packages/core",
		"packages/server",
		"packages/cli",
		"packages/dashboard",
		"packages/modules/*"
	],
	"scripts": {
		"dev": "npm run build -w @simvyn/dashboard & npx tsx packages/cli/src/index.ts --no-open",
		"start": "npx tsx packages/cli/src/index.ts",
		"build": "npm run build -w @simvyn/dashboard",
		"build:all": "npm run build --workspaces --if-present",
		"typecheck": "npx tsc --build",
		"lint": "biome check ."
	},
	"engines": {
		"node": ">=22.12.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2",
		"tsx": "^4.21.0",
		"typescript": "^5.9.3"
	}
}
