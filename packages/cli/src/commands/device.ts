import type { Command } from "commander";

export function registerDeviceCommand(program: Command): void {
	// Placeholder — will be implemented in Task 2
	program.command("device").description("Device management commands");
}
