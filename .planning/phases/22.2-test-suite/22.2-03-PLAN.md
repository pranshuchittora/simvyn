---
phase: 22.2-test-suite
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/__tests__/device-manager.test.ts
  - packages/core/src/__tests__/storage.test.ts
  - packages/core/src/__tests__/verbose-exec.test.ts
autonomous: true
requirements: [TEST-04, TEST-05]

must_haves:
  truths:
    - "DeviceManager polling, fingerprinting, and event emission are tested"
    - "Storage read/write/delete with atomic rename is tested"
    - "verboseExec/verboseSpawn delegate to execFile/spawn correctly"
    - "npm test passes from a clean checkout with zero external dependencies"
  artifacts:
    - path: "packages/core/src/__tests__/device-manager.test.ts"
      provides: "DeviceManager unit tests"
      min_lines: 80
    - path: "packages/core/src/__tests__/storage.test.ts"
      provides: "Storage unit tests"
      min_lines: 60
    - path: "packages/core/src/__tests__/verbose-exec.test.ts"
      provides: "Verbose exec unit tests"
      min_lines: 40
  key_links:
    - from: "packages/core/src/__tests__/device-manager.test.ts"
      to: "packages/core/src/device-manager.ts"
      via: "imports createDeviceManager, provides mock adapters"
      pattern: "createDeviceManager"
    - from: "packages/core/src/__tests__/storage.test.ts"
      to: "packages/core/src/storage.ts"
      via: "imports createModuleStorage, uses temp directory"
      pattern: "createModuleStorage"
---

<objective>
Write unit tests for core utilities: DeviceManager, Storage, and verbose-exec.

Purpose: Verify core infrastructure works correctly — device polling/events, persistent storage, and command execution delegation.
Output: Three test files covering all core utility modules.
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/device-manager.ts
@packages/core/src/storage.ts
@packages/core/src/verbose-exec.ts
@packages/core/src/process-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeviceManager unit tests</name>
  <files>packages/core/src/__tests__/device-manager.test.ts</files>
  <action>
  Create `packages/core/src/__tests__/device-manager.test.ts` using `node:test` and `node:assert`.

  **Strategy:** Create mock PlatformAdapter objects that return controlled device lists. No mocking of verboseExec needed — DeviceManager doesn't call exec directly, it calls adapter.listDevices().

  **Tests:**
  1. **createDeviceManager** — creates a manager with adapters, devices starts empty
  2. **refresh** — calls all adapter listDevices(), returns merged sorted list
  3. **sorting** — booted devices first, then by platform (android < ios), then by name alphabetically
  4. **fingerprinting** — emits `devices-changed` only when device id+state combination changes, NOT on every poll
  5. **start/stop** — start begins polling (verify poll is called), stop clears interval
  6. **setPollInterval** — changes the interval; if already started, restarts the interval timer
  7. **getAdapter** — returns the correct adapter for a given platform, undefined for missing platform
  8. **event emission** — on/off for `devices-changed` works correctly; callback receives sorted device array
  9. **no false positives** — polling same device list twice does NOT emit `devices-changed` a second time

  Use `mock.timers` from `node:test` to control setInterval behavior for polling tests. Alternatively, just test refresh() directly and verify event emission without relying on timers.
  </action>
  <verify>`npm test` passes with all device-manager tests green</verify>
  <done>DeviceManager tests verify: creation, refresh, sorting, fingerprint-based change detection, start/stop, setPollInterval, getAdapter, and event emission</done>
</task>

<task type="auto">
  <name>Task 2: Storage and verbose-exec unit tests</name>
  <files>packages/core/src/__tests__/storage.test.ts, packages/core/src/__tests__/verbose-exec.test.ts</files>
  <action>
  **Storage tests** (`storage.test.ts`):

  Use a temporary directory (via `node:os` tmpdir + `node:fs/promises` mkdtemp) to avoid touching real `~/.simvyn/`. Mock the `SIMVYN_DIR` constant or use `mock.module()` to redirect the homedir.

  Alternatively, since `createModuleStorage` uses `homedir()` internally to compute paths, mock `node:os` `homedir()` to return a temp directory.

  Tests:
  1. **read — missing key** — returns null (no throw)
  2. **write + read** — writes JSON data, reads it back identically
  3. **write creates directory** — module subdirectory created automatically via `mkdir recursive`
  4. **write uses atomic rename** — file written as .tmp then renamed (verify final file exists, no .tmp leftover)
  5. **delete** — removes the key file
  6. **delete — missing key** — does not throw (ENOENT swallowed)
  7. **overwrite** — writing same key twice replaces the value

  **Verbose-exec tests** (`verbose-exec.test.ts`):

  Mock `node:child_process` `execFile` and `spawn` to verify verboseExec/verboseSpawn delegate correctly.

  Tests:
  1. **verboseExec** — calls execFile with correct command + args, returns {stdout, stderr}
  2. **verboseExec error** — re-throws on execFile failure
  3. **verboseSpawn** — calls spawn with correct command + args, returns ChildProcess
  4. **verboseSpawn with options** — passes SpawnOptions through to spawn
  5. **setVerbose** — when verbose=true, commands are logged to stderr (verify process.stderr.write is called)
  6. **setVerbose false** — when verbose=false, no stderr output
  7. **logCommand prefix** — adb/emulator get green [adb] prefix, xcrun gets blue [simctl] prefix, others get no prefix

  Clean up temp directories in afterEach/after hooks.
  </action>
  <verify>`npm test` passes with all storage and verbose-exec tests green</verify>
  <done>Storage tests verify read/write/delete with atomic rename and error handling. Verbose-exec tests verify command delegation, error propagation, and verbose logging with colored prefixes.</done>
</task>

</tasks>

<verification>
- `npm test` runs all test files and reports zero failures
- Storage tests don't touch ~/.simvyn/ (use temp directory)
- Verbose-exec tests don't execute real commands (mocked child_process)
- DeviceManager tests don't require real simulators/emulators
</verification>

<success_criteria>
- DeviceManager: 9 test cases covering polling, sorting, fingerprinting, events
- Storage: 7 test cases covering CRUD + atomic write + error handling
- Verbose-exec: 7 test cases covering delegation, errors, and verbose logging
- All tests pass with `npm test` — zero external dependencies needed
</success_criteria>

<output>
After completion, create `.planning/phases/22.2-test-suite/22.2-03-SUMMARY.md`
</output>
