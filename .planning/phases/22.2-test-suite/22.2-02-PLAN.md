---
phase: 22.2-test-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/__tests__/android-adapter.test.ts
autonomous: true
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "Every Android adapter method has a test verifying the correct adb/emulator command and arguments"
    - "Tests mock verboseExec/verboseSpawn — no real devices needed"
    - "Android-specific behaviors tested: avd: prefix guards, geo fix lon,lat order, permission prefix expansion"
  artifacts:
    - path: "packages/core/src/__tests__/android-adapter.test.ts"
      provides: "Android adapter unit tests"
      min_lines: 400
  key_links:
    - from: "packages/core/src/__tests__/android-adapter.test.ts"
      to: "packages/core/src/adapters/android.ts"
      via: "imports createAndroidAdapter, mocks verboseExec/verboseSpawn"
      pattern: "mock.*verbose"
---

<objective>
Write comprehensive Android adapter tests covering all ~45 methods with mocked adb/emulator commands.

Purpose: Verify every adb command is correctly constructed, especially Android-specific quirks (avd: prefix guards, geo fix lon/lat order, permission prefix expansion).
Output: android-adapter.test.ts covering all Android adapter methods.
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/adapters/android.ts
@packages/core/src/verbose-exec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android adapter comprehensive tests</name>
  <files>packages/core/src/__tests__/android-adapter.test.ts</files>
  <action>
  Create `packages/core/src/__tests__/android-adapter.test.ts` using `node:test` and `node:assert`.

  **Mocking strategy:** Same as iOS tests — use `mock.module()` to replace `../verbose-exec.js` module before importing the adapter. Track all calls in a shared array, reset between tests.

  **Test structure — one describe per method category:**

  1. **isAvailable** — calls `adb version`, returns true on success, false on error
  2. **listDevices** — complex test:
     - Mock `emulator -list-avds` returning AVD names
     - Mock `adb devices` returning serial lines
     - Mock `adb -s <serial> emu avd name` for emulator name resolution
     - Mock `adb -s <serial> shell getprop` for model/version
     - Verify: booted emulators get emulator-XXXX serial, physical devices get their serial, shutdown AVDs get `avd:` prefix, bootedAvdNames dedup works
  3. **boot** — verify:
     - Throws for non-`avd:` prefix IDs
     - Spawns `emulator @<name>` with detached + stdio:ignore
     - Polls adb devices until avd name matches (mock the polling loop)
  4. **shutdown** — `adb -s <id> emu kill`, no-op for `avd:` prefix
  5. **erase** — verify it's `undefined` (not available on Android)
  6. **setLocation** — `adb -s <id> emu geo fix <LON> <LAT>` (note: lon FIRST, lat second — this is the Android geo fix quirk)
  7. **clearLocation** — `adb -s <id> emu geo fix 0 0`
  8. **listApps** — `adb -s <id> shell pm list packages -f -3`, verify parsing of `package:/path=com.example` format, throws for `avd:` prefix
  9. **installApp** — `adb -s <id> install <path>`, throws for `avd:` prefix
  10. **uninstallApp** — `adb -s <id> uninstall <bundleId>`, throws for `avd:` prefix
  11. **launchApp** — `adb -s <id> shell monkey -p <bundleId> -c android.intent.category.LAUNCHER 1`, throws for `avd:` prefix
  12. **terminateApp** — `adb -s <id> shell am force-stop <bundleId>`, throws for `avd:` prefix
  13. **getAppInfo** — `adb -s <id> shell dumpsys package <bundleId>`, verify versionName/versionCode/dataDir/codePath parsing, returns null on error
  14. **clearAppData** — `adb -s <id> shell pm clear <bundleId>`, throws for `avd:` prefix
  15. **openUrl** — `adb -s <id> shell am start -a android.intent.action.VIEW -d <url>`, throws for `avd:` prefix
  16. **screenshot** — three sequential commands: screencap to /sdcard, pull, rm; throws for `avd:` prefix
  17. **startRecording** — spawns `adb -s <id> shell screenrecord /sdcard/simvyn_recording.mp4`, stashes deviceId/outputPath on child; rejects for `avd:` prefix
  18. **stopRecording** — kills child with SIGINT, pulls recording, cleans up remote file
  19. **getClipboard** — verify it's `undefined` (not available on Android)
  20. **setClipboard** — tries `adb -s <id> shell cmd clipboard set-text <text>` first, falls back to `adb -s <id> shell input text <escaped>` on failure; throws for `avd:` prefix
  21. **setAppearance** — `adb -s <id> shell cmd uimode night yes/no`, throws for `avd:` prefix
  22. **addMedia** — `adb -s <id> push <path> /sdcard/DCIM/<filename>` then broadcast MEDIA_SCANNER_SCAN_FILE; throws for `avd:` prefix
  23. **grantPermission** — `adb -s <id> shell pm grant <bundleId> <perm>`; verify auto-prefix: `CAMERA` → `android.permission.CAMERA`, already-prefixed left alone; throws for `avd:` prefix
  24. **revokePermission** — same pattern as grant with `revoke`
  25. **resetPermissions** — verify it's `undefined`
  26. **setLocale** — `adb -s <id> shell su 0 setprop persist.sys.locale <bcp47>` then `ctl.restart zygote`; verify underscore→hyphen conversion (en_US → en-US); throws for `avd:` prefix
  27. **setTalkBack** — two `settings put secure` calls: enabled_accessibility_services + accessibility_enabled; throws for `avd:` prefix
  28. **setStatusBar/clearStatusBar/setContentSize/setIncreaseContrast** — verify all are `undefined`
  29. **addForward** — `adb -s <id> forward <local> <remote>`, throws for `avd:` prefix
  30. **removeForward** — `adb -s <id> forward --remove <local>`, throws for `avd:` prefix
  31. **listForwards** — `adb -s <id> forward --list`, verify parsing of `serial local remote` lines
  32. **addReverse** — `adb -s <id> reverse <remote> <local>`, throws for `avd:` prefix
  33. **removeReverse** — `adb -s <id> reverse --remove <remote>`, throws for `avd:` prefix
  34. **listReverses** — `adb -s <id> reverse --list`, verify parsing
  35. **setDisplaySize** — `adb -s <id> shell wm size <W>x<H>`, throws for `avd:` prefix
  36. **resetDisplaySize** — `adb -s <id> shell wm size reset`, throws for `avd:` prefix
  37. **setDisplayDensity** — `adb -s <id> shell wm density <dpi>`, throws for `avd:` prefix
  38. **resetDisplayDensity** — `adb -s <id> shell wm density reset`, throws for `avd:` prefix
  39. **setBattery** — conditional `dumpsys battery set` calls for level/status/ac/usb, throws for `avd:` prefix
  40. **unplugBattery** — `adb -s <id> shell dumpsys battery unplug`, throws for `avd:` prefix
  41. **resetBattery** — `adb -s <id> shell dumpsys battery reset`, throws for `avd:` prefix
  42. **inputTap** — `adb -s <id> shell input tap <x> <y>`, throws for `avd:` prefix
  43. **inputSwipe** — `adb -s <id> shell input swipe <x1> <y1> <x2> <y2> [duration]`, verify optional duration
  44. **inputText** — `adb -s <id> shell input text <escaped>`, verify space→%s and special char escaping
  45. **inputKeyEvent** — `adb -s <id> shell input keyevent <code>`
  46. **collectBugReport** — `adb -s <id> bugreport <path>` with 300s timeout, throws for `avd:` prefix
  47. **capabilities** — returns expected PlatformCapability array (no push, no erase, no statusBar, etc.)

  **Key Android quirks to test explicitly:**
  - `avd:` prefix guard: ~20 methods should throw "Device must be booted" for `avd:` prefixed IDs
  - `geo fix` uses lon,lat order (not lat,lon)
  - Permission auto-prefix: short `CAMERA` → `android.permission.CAMERA`
  - Locale underscore→hyphen conversion
  - `monkey` for app launch (not am start)
  - Screenshot is 3 sequential commands (screencap + pull + rm)
  </action>
  <verify>`npm test` passes with all Android adapter tests green, zero failures</verify>
  <done>Every Android adapter method has at least one test verifying exact adb/emulator command and arguments. avd: prefix guards tested. Android-specific quirks (lon/lat order, permission prefix, locale format) verified.</done>
</task>

</tasks>

<verification>
- `npm test` runs and all Android adapter tests pass
- No real adb/emulator commands executed during tests
- Android-specific edge cases covered (avd: guards, geo fix order, permission prefix)
</verification>

<success_criteria>
- Android adapter has tests for every public method (~45 methods)
- All avd: prefix guard tests pass (methods throw for unbooted AVDs)
- Geo fix lon,lat order verified
- Permission prefix auto-expansion verified
- All tests pass with mocked exec/spawn
</success_criteria>

<output>
After completion, create `.planning/phases/22.2-test-suite/22.2-02-SUMMARY.md`
</output>
