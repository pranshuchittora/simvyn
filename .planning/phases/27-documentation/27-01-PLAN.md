---
phase: 27-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "README opens with logo, tagline, quick-start, and visual feature overview — purpose clear within 30 seconds"
    - "Each module has a dedicated showcase section with description and screenshot placeholder"
    - "Collections feature has a getting-started walkthrough explaining create, configure, and apply workflow"
    - "CLI reference table lists every command with usage examples, covering all modules including collections"
  artifacts:
    - path: "README.md"
      provides: "Comprehensive project documentation"
      min_lines: 300
  key_links: []
---

<objective>
Restructure the README into a comprehensive, visual-first documentation page with per-module showcases, collections walkthrough, and complete CLI reference.

Purpose: New users need to understand what simvyn does, how to install it, and how every feature works — from a single README that serves as the primary documentation.
Output: Expanded README.md (~350-500 lines) with logo, features overview, per-module showcases, collections guide, and CLI reference.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@README.md
@packages/modules/collections/action-registry.ts
@packages/modules/collections/manifest.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README with module showcases, collections guide, and CLI reference</name>
  <files>
    README.md
  </files>
  <action>
Rewrite README.md with the following structure. Keep the existing logo, tagline, badges, banner, and demo GIF from the current README (first ~22 lines). Then restructure everything below:

**Section structure:**

1. **Header** (keep existing): Logo, title, tagline, npm/license badges, banner image, demo GIF

2. **Quick Start** (keep existing): `npx simvyn` with brief description

3. **Installation** (keep existing): Global install, npx, requirements

4. **Features Overview** — Keep the existing bullet list but reformat as a cleaner table or keep as concise feature list. This is the "30-second understanding" section.

5. **Module Showcases** — Each module gets a dedicated subsection with:
   - ### heading with the module name
   - 2-3 sentence description of what it does and why it's useful
   - Screenshot placeholder: `<p align="center"><img src="https://raw.githubusercontent.com/pranshuchittora/simvyn/main/assets/screenshots/{module-slug}.png" alt="{module name}" width="700" /></p>`
   - Key capabilities as a short bullet list (3-5 items)

   Create showcase sections for these modules (in order):
   - **Device Management** — Discover, boot, shutdown, erase simulators/emulators. Create and clone iOS simulators.
   - **Location** — Interactive map, GPS coordinates, GPX/KML route playback with speed control, favorites.
   - **App Management** — Install, launch, terminate apps. Drag-and-drop IPA/APK. View app info and clear data.
   - **Log Viewer** — Real-time streaming, level filtering, regex search, process filtering, export, pagination.
   - **Screenshots & Recording** — Capture screenshots, record screen video, history with timestamps.
   - **Deep Links** — Open URLs and custom schemes, save favorites per app.
   - **Push Notifications** — Compose JSON payloads, send to iOS simulators, template library.
   - **File Browser** — Browse app sandboxes, upload/download files, inline text editing.
   - **Database Inspector** — SQLite tables, SQL queries, SharedPreferences/NSUserDefaults viewing.
   - **Device Settings** — Dark/light mode, locale, permissions, status bar overrides, accessibility presets.
   - **Crash Logs** — iOS diagnostic reports, Android logcat/tombstone crashes.
   - **Developer Utilities** — Port forwarding, display overrides, battery simulation, input text, bug reports.
   - **Collections** — Create reusable sets of device actions, apply to multiple devices at once. Include starter presets.

6. **Collections Guide** — A getting-started walkthrough:
   - ### Collections
   - Brief intro: "Collections let you bundle multiple device actions into a reusable sequence and apply them to any combination of devices with one click."
   - **Creating a Collection**: Open collections panel, click New Collection, name it, add steps from the action catalog
   - **Adding Steps**: Browse categorized actions (Device Settings, Location, App Management, etc.), configure parameters inline, drag to reorder
   - **Applying a Collection**: Click Apply, select target devices, see compatibility summary (skipped steps for incompatible platforms), execute with Cmd+Enter. Live status matrix shows per-step per-device progress.
   - **Command Palette**: Saved collections appear in Cmd+K — quick-launch with device picker
   - **Starter Collections**: 3 built-in presets ship with simvyn: "Dark Mode + Japanese Locale", "Screenshot Setup", "Reset Device State"
   - **CLI Usage**: `simvyn collections list`, `simvyn collections apply <name> <devices...>`

7. **CLI Reference** — Comprehensive table with ALL commands:

   Format as a table: `| Command | Description |`

   Include ALL commands from all modules:
   ```
   simvyn                                          Start dashboard (default)
   simvyn device list                              List all devices
   simvyn device boot <device>                     Boot a device
   simvyn device shutdown <device>                 Shutdown a device
   simvyn device erase <device>                    Erase device content
   simvyn device create <name> <type> <runtime>    Create iOS simulator
   simvyn device clone <device> <name>             Clone iOS simulator
   simvyn device rename <device> <name>            Rename iOS simulator
   simvyn device delete <device>                   Delete iOS simulator
   simvyn location set <device> <lat> <lng>        Set GPS coordinates
   simvyn location route <device> <file>           Play GPX/KML route
   simvyn location clear <device>                  Clear GPS location
   simvyn app list <device>                        List installed apps
   simvyn app install <device> <path>              Install IPA/APK
   simvyn app uninstall <device> <bundle-id>       Uninstall an app
   simvyn app launch <device> <bundle-id>          Launch an app
   simvyn app terminate <device> <bundle-id>       Terminate an app
   simvyn app info <device> <bundle-id>            Show app details
   simvyn app clear-data <device> <bundle-id>      Clear app data (Android)
   simvyn logs <device>                            Stream device logs
   simvyn screenshot <device>                      Capture screenshot
   simvyn record <device>                          Record screen
   simvyn link <device> <url>                      Open deep link
   simvyn push <device>                            Send push notification
   simvyn fs ls <device> <bundle-id> [path]        List files
   simvyn fs pull <device> <bundle-id> <path>      Download file
   simvyn fs push <device> <bundle-id> <src> <dst> Upload file
   simvyn db list <device> <bundle-id>             List databases
   simvyn db query <device> <bundle-id> <db> <sql> Run SQL query
   simvyn db prefs <device> <bundle-id>            View preferences
   simvyn keychain add <device> <cert>             Add root certificate
   simvyn keychain reset <device>                  Reset keychain
   simvyn collections list                         List collections
   simvyn collections show <id>                    Show collection details
   simvyn collections create <name>                Create collection
   simvyn collections delete <id>                  Delete collection
   simvyn collections duplicate <id>               Duplicate collection
   simvyn collections apply <name> <devices...>    Apply collection
   ```

8. **How It Works** (keep existing but slightly expand): Mention the module/plugin architecture, WebSocket multiplexing, and zero-SDK approach.

9. **Supported Platforms** (keep existing table)

10. **License** (keep existing)

**Style rules:**
- Use HTML `<p align="center">` for images (renders on both npm and GitHub)
- Screenshot placeholder paths: `assets/screenshots/{module-slug}.png`
- No emojis — clean, professional technical documentation
- Keep descriptions concise — each module showcase 5-10 lines max
- CLI table formatted with consistent column widths
- Maintain existing tone (direct, developer-friendly)
  </action>
  <verify>
Verify README.md exists and has 300+ lines. Check that it includes sections for: Quick Start, Features, module showcases (13 modules), Collections guide, CLI Reference table (30+ commands), Supported Platforms.
  </verify>
  <done>
README restructured with logo/badges header, feature overview, 13 per-module showcase sections with screenshot placeholders, collections getting-started walkthrough, comprehensive CLI reference table (30+ commands), platform matrix. Purpose clear within 30 seconds of opening.
  </done>
</task>

</tasks>

<verification>
1. README.md has 300+ lines
2. Header section has logo, tagline, badges, banner, demo GIF
3. 13 module showcase sections each have description, screenshot placeholder, capability list
4. Collections guide has create → configure → apply workflow walkthrough
5. CLI reference table has 30+ commands with descriptions
6. All collections CLI commands included (list, show, create, delete, duplicate, apply)
</verification>

<success_criteria>
- DOC-01: Visual-first layout with logo, quick start, features, per-feature showcase, CLI reference
- DOC-02: Per-feature showcase sections with description and screenshot placeholder
- DOC-03: Collections getting-started walkthrough
- DOC-04: Expanded CLI reference table with all commands
</success_criteria>

<output>
After completion, create `.planning/phases/27-documentation/27-01-SUMMARY.md`
</output>
