---
phase: 11-location-module-rewrite
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/main.css
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/components/TopBar.tsx
  - packages/dashboard/src/components/DeviceSelector.tsx
  - packages/dashboard/src/components/Sidebar.tsx
  - packages/dashboard/src/components/ModuleShell.tsx
autonomous: true
requirements: [LOC-REWRITE-03]

must_haves:
  truths:
    - "Dashboard body matches sim-location's deep dark background (#0a0a0f)"
    - "Glass panels have inset highlight (inset 0 0.5px 0 rgba(255,255,255,0.06)) and deep shadow (0 4px 24px rgba(0,0,0,0.3))"
    - ".glass-button class exists and matches sim-location's button styling"
    - "Glass input and select elements have consistent dark styling"
    - "TopBar has frosted glass treatment matching sim-location's panel aesthetic"
    - "DeviceSelector dropdown uses glass-panel styling with proper hover states"
    - "Toasts use sim-location's glass toast styling"
    - "All existing panels (Device, App, Log, etc.) automatically look better through updated tokens"
  artifacts:
    - path: "packages/dashboard/src/main.css"
      provides: "Updated design system with sim-location glass effects"
      contains: "glass-button"
    - path: "packages/dashboard/src/components/TopBar.tsx"
      provides: "Glass-treated header bar"
      contains: "TopBar"
    - path: "packages/dashboard/src/components/DeviceSelector.tsx"
      provides: "Glass dropdown with polished hover states"
      contains: "DeviceSelector"
    - path: "packages/dashboard/src/App.tsx"
      provides: "Updated toast styling to match glass system"
      contains: "Toaster"
  key_links:
    - from: "packages/dashboard/src/main.css"
      to: "all panel components"
      via: "Tailwind @theme tokens and utility classes"
      pattern: "glass-panel"
---

<objective>
Update the common dashboard UI foundation to match sim-location's proven liquid glass implementation — richer glass panels with inset highlights, deeper shadows, polished buttons/inputs, and consistent dark aesthetic.

Purpose: sim-location has a perfected glassmorphism design system. Currently simvyn's dashboard has simpler glass effects without the inset highlight, deep box-shadow, or polished form elements. Updating the foundation CSS improves ALL module panels automatically (they all use .glass-panel, bg-bg-surface, etc.).

Output: Updated main.css design system + polished shell components (TopBar, DeviceSelector, Sidebar, ModuleShell, App)
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source reference (sim-location's proven glass CSS):
@/Users/pranshu/github/sim-location/src/web/styles/map.css

Current dashboard files to update:
@packages/dashboard/src/main.css
@packages/dashboard/src/App.tsx
@packages/dashboard/src/components/TopBar.tsx
@packages/dashboard/src/components/DeviceSelector.tsx
@packages/dashboard/src/components/Sidebar.tsx
@packages/dashboard/src/components/ModuleShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main.css design system to match sim-location glass aesthetic</name>
  <files>
    packages/dashboard/src/main.css
  </files>
  <action>
Update main.css to incorporate sim-location's proven glass effects while keeping the Tailwind @theme token system (all panels reference these tokens).

**@theme token updates** — Tune colors to match sim-location's proven values:
```
--color-bg-base: oklch(0.05 0.01 270);           /* was 0.08 — darker, matching #0a0a0f */
--color-bg-surface: oklch(0.12 0.012 270);        /* subtle tune */
--color-bg-elevated: oklch(0.16 0.015 270);       /* subtle tune */
--color-glass: oklch(0.14 0.015 270 / 0.55);      /* was 0.6 — matches rgba(30,30,40,0.55) */
--color-glass-hover: oklch(0.18 0.015 270 / 0.7); /* tune to match */
--color-glass-border: oklch(1 0 0 / 0.08);        /* was oklch(0.35...) — matches rgba(255,255,255,0.08) */
--color-glass-border-hover: oklch(1 0 0 / 0.18);  /* matches rgba(255,255,255,0.18) */
--color-text-primary: oklch(0.93 0.005 260);       /* matches #e8e8ed */
--color-text-secondary: oklch(0.65 0.005 260);     /* tune */
--color-text-muted: oklch(0.4 0.005 260);          /* tune */
--color-border: oklch(1 0 0 / 0.08);               /* was oklch(0.25...) — match glass-border */
```

**Body background** — Update gradient to be deeper/darker, matching sim-location's `#0a0a0f`:
```css
body {
  background: linear-gradient(
    145deg,
    oklch(0.04 0.02 280) 0%,
    oklch(0.05 0.015 260) 30%,
    oklch(0.045 0.012 240) 60%,
    oklch(0.06 0.02 270) 100%
  );
  /* keep existing font properties */
}
```

**Update .glass-panel** — Add sim-location's inset highlight and box-shadow:
```css
.glass-panel {
  backdrop-filter: blur(20px) saturate(1.4);           /* was blur(24px) saturate(1.3) — match sim-location */
  -webkit-backdrop-filter: blur(20px) saturate(1.4);   /* Safari support */
  background: var(--color-glass);
  border: 1px solid var(--color-glass-border);
  border-radius: var(--radius-panel);
  box-shadow:
    0 4px 24px rgba(0, 0, 0, 0.3),                    /* NEW: deep shadow */
    inset 0 0.5px 0 rgba(255, 255, 255, 0.06);        /* NEW: top highlight */
  color: var(--color-text-primary);
}
```

**Add .glass-button** — Matching sim-location's polished button:
```css
.glass-button {
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-button);
  padding: 6px 12px;
  color: var(--color-text-primary);
  cursor: pointer;
  font-size: 0.75rem;
  font-family: inherit;
  transition: background 0.2s ease, border-color 0.2s ease;
}

.glass-button:hover {
  background: rgba(255, 255, 255, 0.14);
  border-color: rgba(255, 255, 255, 0.18);
}

.glass-button:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}
```

**Add glass form elements:**
```css
.glass-input {
  background: rgba(0, 0, 0, 0.25);
  color: var(--color-text-primary);
  border: 1px solid rgba(255, 255, 255, 0.1);
  padding: 8px 12px;
  border-radius: var(--radius-button);
  font-size: 0.85rem;
  font-family: inherit;
}

.glass-input::placeholder {
  color: rgba(255, 255, 255, 0.3);
}

.glass-input:focus {
  border-color: rgba(0, 180, 255, 0.5);
  box-shadow: 0 0 0 2px rgba(0, 180, 255, 0.15);
  outline: none;
}

.glass-select {
  background: rgba(0, 0, 0, 0.25);
  color: var(--color-text-primary);
  border: 1px solid rgba(255, 255, 255, 0.1);
  padding: 6px 10px;
  border-radius: var(--radius-button);
  font-size: 0.8rem;
  cursor: pointer;
  font-family: inherit;
}

.glass-select option {
  background: #1e1e28;
  color: #e8e8ed;
}
```

**Update dock-sidebar** — Tune to match sim-location's dark glass:
```css
.dock-sidebar {
  /* keep existing flex/width properties */
  backdrop-filter: blur(20px) saturate(1.4);  /* was blur(24px) saturate(1.3) */
  background: rgba(16, 16, 24, 0.7);          /* was oklch — match sim-location dark */
  border-right: 1px solid rgba(255, 255, 255, 0.08);
}
```

**Update dock-tooltip** — Glass treatment:
```css
.dock-icon .dock-tooltip {
  backdrop-filter: blur(20px) saturate(1.4);
  background: rgba(30, 30, 40, 0.9);
  border: 1px solid rgba(255, 255, 255, 0.08);
  /* keep existing positioning and transition */
}
```

**Update Sonner toast overrides** — Match sim-location exactly:
```css
[data-sonner-toaster] [data-sonner-toast] {
  background: rgba(30, 30, 40, 0.7) !important;
  backdrop-filter: blur(20px) saturate(1.4) !important;
  -webkit-backdrop-filter: blur(20px) saturate(1.4) !important;
  border: 1px solid rgba(255, 255, 255, 0.08) !important;
  color: #e8e8ed !important;
  border-radius: 12px !important;
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.3) !important;
}

[data-sonner-toaster] [data-sonner-toast] [data-description] {
  color: rgba(255, 255, 255, 0.5) !important;
}
```

Keep the existing scrollbar styles and dock-icon animations — they're already good.

IMPORTANT: Keep `@import "tailwindcss";` at the top. Keep all Tailwind @theme tokens (panels reference them). The tokens are being TUNED, not removed.
  </action>
  <verify>
Run `npm run dev:dashboard` (or check that CSS compiles) — the dashboard should load with visibly deeper/darker background and richer glass panels.
  </verify>
  <done>main.css has tuned dark tokens, .glass-panel with inset highlight + deep shadow, .glass-button, .glass-input, .glass-select, updated dock-sidebar, updated toast overrides. All existing panels automatically benefit from updated tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Update shell components for polished glass treatment</name>
  <files>
    packages/dashboard/src/App.tsx
    packages/dashboard/src/components/TopBar.tsx
    packages/dashboard/src/components/DeviceSelector.tsx
    packages/dashboard/src/components/Sidebar.tsx
    packages/dashboard/src/components/ModuleShell.tsx
  </files>
  <action>
**App.tsx** — Update Toaster `toastOptions.style` to use sim-location's rgba glass values instead of oklch inline styles:
```tsx
style: {
  background: "rgba(30, 30, 40, 0.7)",
  backdropFilter: "blur(20px) saturate(1.4)",
  WebkitBackdropFilter: "blur(20px) saturate(1.4)",
  border: "1px solid rgba(255, 255, 255, 0.08)",
  borderRadius: "12px",
  color: "#e8e8ed",
  fontSize: "13px",
  fontFamily: "-apple-system, BlinkMacSystemFont, 'SF Pro Display', Inter, sans-serif",
  boxShadow: "0 4px 24px rgba(0, 0, 0, 0.3), inset 0 0.5px 0 rgba(255, 255, 255, 0.06)",
}
```

**TopBar.tsx** — Replace inline oklch Tailwind classes with CSS-class-based glass treatment. Replace the header's className:
```
"relative z-30 flex h-12 shrink-0 items-center justify-between px-4"
```
And add a dedicated CSS class `.top-bar` in main.css (or keep inline but use sim-location's values):
```
backdrop-filter: blur(20px) saturate(1.4);
background: rgba(16, 16, 24, 0.7);
border-bottom: 1px solid rgba(255, 255, 255, 0.08);
```
The simvyn logo gradient can stay (`bg-gradient-to-r from-accent-blue to-accent-purple bg-clip-text text-transparent`). Keep connection indicator as-is.

**DeviceSelector.tsx** — Polish the dropdown:
- Trigger button: Use `glass-button` class or sim-location-style rgba values instead of raw Tailwind `bg-bg-surface/50 border border-glass-border`. Give it the inset highlight shadow.
- Dropdown panel: Already uses `glass-panel` (good — will inherit the updated styles from Task 1). Ensure hover states on items use `rgba(255, 255, 255, 0.08)` pattern.
- Active item: Keep accent-blue highlight — it works well.

**Sidebar.tsx** — Already uses `.dock-sidebar` CSS class (updated in Task 1). No major changes needed. Minor: ensure the dock-icon hover background uses `rgba(255, 255, 255, 0.08)` pattern. Update the `.dock-icon:hover` background in main.css to `rgba(255, 255, 255, 0.08)` (was `oklch(0.2 0.02 260 / 0.5)`). Update `.dock-icon.active` background to `rgba(0, 122, 255, 0.15)` (matching sim-location's accent pattern).

**ModuleShell.tsx** — Minor: Update `LoadingSkeleton` to use glass-panel with updated styles. The bg-bg-surface/60 and animate-pulse patterns are fine as-is since they reference updated tokens.

IMPORTANT: Don't break any existing functionality. These are purely visual refinements. Keep all event handlers, state, and logic unchanged. Only CSS classes and style values change.
  </action>
  <verify>
1. Run `npm run dev:dashboard` — dashboard loads without errors
2. Visual check: background is deeper/darker, glass panels have visible inner highlight, DeviceSelector dropdown looks glassy, TopBar has frosted effect
3. Navigate to Device, App, Log panels — they should all look slightly more polished (deeper glass, better shadows)
  </verify>
  <done>
Dashboard shell (TopBar, DeviceSelector, Sidebar) and all panels using .glass-panel now render with sim-location's polished glass aesthetic: inset highlight, deep shadow, consistent dark rgba backgrounds, polished hover states. The design system is consistent with the location panel CSS.
  </done>
</task>

</tasks>

<verification>
- main.css has .glass-button, .glass-input, .glass-select utility classes
- .glass-panel includes `box-shadow: 0 4px 24px rgba(0,0,0,0.3), inset 0 0.5px 0 rgba(255,255,255,0.06)`
- Body background is deeper/darker than before
- TopBar uses glass treatment (not raw oklch inline values)
- DeviceSelector dropdown inherits updated glass-panel styles
- Toast styling matches sim-location's rgba glass values
- All existing module panels (Device, App, Log, Screenshot, etc.) automatically benefit from updated tokens
- No functionality regressions — only visual improvements
</verification>

<success_criteria>
The entire dashboard visually matches sim-location's glass aesthetic. Every panel, dropdown, and toolbar has the same polished frosted-glass look with inset highlights, deep shadows, and consistent dark backgrounds. The location panel (plans 01-03) and the rest of the dashboard share a unified glass design system.
</success_criteria>

<output>
After completion, create `.planning/phases/11-location-module-rewrite/11-04-SUMMARY.md`
</output>
