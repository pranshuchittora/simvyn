---
phase: 11-location-module-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/panels/location/stores/location-store.ts
  - packages/dashboard/src/panels/location/stores/playback-store.ts
  - packages/dashboard/src/panels/location/stores/route-store.ts
  - packages/dashboard/src/panels/location/stores/favorites-store.ts
  - packages/dashboard/src/panels/location/utils/route-parser.ts
autonomous: true
requirements: [LOC-REWRITE-01, LOC-REWRITE-02]

must_haves:
  truths:
    - "Location store exposes markerPosition, cursorPosition, myLocation, reverseGeocode, searchResults, searchQuery"
    - "Playback store exposes speedKmh, speedUnit, multiplier, loop, SPEED_PRESETS, MULTIPLIERS, kmhToMs, msToKmh"
    - "Route store exposes interactionMode, updateWaypoint, clearRoute"
    - "Favorites store supports saveLocation with address/emoji fields, saveRoute, deleteLocation, deleteRoute"
    - "Route parser handles GPX and KML files via @tmcw/togeojson"
  artifacts:
    - path: "packages/dashboard/src/panels/location/stores/location-store.ts"
      provides: "Full location state with markerPosition, cursor, search, reverse geocode"
      contains: "markerPosition"
    - path: "packages/dashboard/src/panels/location/stores/playback-store.ts"
      provides: "Real-world speed model with presets, multipliers, units"
      contains: "SPEED_PRESETS"
    - path: "packages/dashboard/src/panels/location/stores/route-store.ts"
      provides: "Route state with interactionMode and updateWaypoint"
      contains: "interactionMode"
    - path: "packages/dashboard/src/panels/location/stores/favorites-store.ts"
      provides: "Favorites CRUD with emoji and address support"
      contains: "emoji"
    - path: "packages/dashboard/src/panels/location/utils/route-parser.ts"
      provides: "GPX/KML parsing via @tmcw/togeojson"
      contains: "parseRouteFile"
  key_links:
    - from: "packages/dashboard/src/panels/location/stores/favorites-store.ts"
      to: "/api/modules/location/favorites/*"
      via: "fetch calls"
      pattern: "api/modules/location/favorites"
---

<objective>
Replace all 4 location panel zustand stores with full-featured versions migrated from sim-location, and add the route-parser utility.

Purpose: The current stores are simplified scaffolds missing critical state (markerPosition, cursorPosition, speed presets, interactionMode, emoji support). Components in plans 02 and 03 depend on these richer store interfaces.

Output: 4 replaced store files + 1 new utility file
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source reference (copy from, adapt API paths):
@/Users/pranshu/github/sim-location/src/web/stores/location-store.ts
@/Users/pranshu/github/sim-location/src/web/stores/playback-store.ts
@/Users/pranshu/github/sim-location/src/web/stores/route-store.ts
@/Users/pranshu/github/sim-location/src/web/stores/favorites-store.ts
@/Users/pranshu/github/sim-location/src/web/utils/route-parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all 4 zustand stores with sim-location versions</name>
  <files>
    packages/dashboard/src/panels/location/stores/location-store.ts
    packages/dashboard/src/panels/location/stores/playback-store.ts
    packages/dashboard/src/panels/location/stores/route-store.ts
    packages/dashboard/src/panels/location/stores/favorites-store.ts
  </files>
  <action>
Replace each store file with the sim-location version, adapting as follows:

**location-store.ts** — Copy from sim-location exactly. Interface includes: `markerPosition: [number, number] | null`, `cursorPosition: [number, number] | null`, `myLocation: [number, number] | null`, `reverseGeocode: string | null`, `searchResults: SearchResult[]`, `searchQuery: string`. Export the `SearchResult` interface. All setters: `setMarkerPosition`, `setCursorPosition`, `setMyLocation`, `clearMarkerPosition`, `setReverseGeocode`, `setSearchResults`, `setSearchQuery`.

**playback-store.ts** — Copy from sim-location exactly. Exports: `PlaybackStatus` type, `SpeedUnit` type, `SpeedPreset` interface, `SPEED_PRESETS` array (Walking 5, Cycling 20, Driving 50, Train 160, Plane 900), `MULTIPLIERS` const array [1,2,5,10], `kmhToMs()`, `msToKmh()`. State includes: `status`, `progress`, `currentPosition: [number, number] | null`, `speedKmh`, `speedUnit`, `multiplier`, `loop`. Methods: `setStatus`, `setProgress`, `setCurrentPosition`, `setSpeedKmh`, `setSpeedUnit`, `setMultiplier`, `setLoop`, `reset`.

**route-store.ts** — Copy from sim-location exactly. Adds `interactionMode: InteractionMode` ('point' | 'route'), `updateWaypoint(index, pos)`, and `clearRoute`. Export `InteractionMode` type.

**favorites-store.ts** — Copy from sim-location but change ALL fetch URLs from `/api/favorites/*` to `/api/modules/location/favorites/*`. The `SavedLocation` interface gains `address?: string` and `emoji?: string` fields. `saveLocation` method accepts `{name, lat, lon, address?, emoji?}`. `saveRoute` accepts `{name, waypoints}`. After save, re-fetch list from server (matching sim-location pattern). The `createdAt` field changes from `number` to `string` to match sim-location's ISO format.

Do NOT add a device-store — simvyn already has a global device store at `../stores/device-store`.
  </action>
  <verify>
Run `npx tsc --noEmit -p packages/dashboard/tsconfig.json 2>&1 | head -30` — expect type errors in component files that still use old store interfaces (this is expected and will be fixed in plan 02/03), but the store files themselves should have no internal errors.
  </verify>
  <done>All 4 store files contain the full sim-location store interfaces and logic, with simvyn API paths for favorites. All types and helpers are exported.</done>
</task>

<task type="auto">
  <name>Task 2: Add route-parser utility</name>
  <files>
    packages/dashboard/src/panels/location/utils/route-parser.ts
  </files>
  <action>
Create `packages/dashboard/src/panels/location/utils/` directory and add `route-parser.ts` — copy from `sim-location/src/web/utils/route-parser.ts` exactly. It imports `gpx` and `kml` from `@tmcw/togeojson`, uses browser `DOMParser`, exports `parseRouteFile(text, format)` and `detectRouteFormat(filename)`. The `RouteFileFormat` type is also exported. No changes needed — `@tmcw/togeojson` is already in dashboard's package.json.
  </action>
  <verify>
Run `npx tsc --noEmit -p packages/dashboard/tsconfig.json 2>&1 | grep route-parser` — no errors from route-parser.ts itself.
  </verify>
  <done>route-parser.ts exists with GPX/KML parsing, detectRouteFormat, and RouteFileFormat type exported.</done>
</task>

</tasks>

<verification>
- All 5 files exist and compile individually
- Store interfaces match sim-location exactly (modulo API path changes in favorites-store)
- `SPEED_PRESETS`, `MULTIPLIERS`, `kmhToMs`, `msToKmh` are exported from playback-store
- `SearchResult`, `InteractionMode`, `RouteFileFormat` types are exported
- favorites-store fetch URLs all use `/api/modules/location/` prefix
</verification>

<success_criteria>
All 4 stores and the route-parser utility are production-quality copies of sim-location code, adapted only for simvyn's API path convention. Components (plan 02/03) can import from these stores and get the full feature set.
</success_criteria>

<output>
After completion, create `.planning/phases/11-location-module-rewrite/11-01-SUMMARY.md`
</output>
