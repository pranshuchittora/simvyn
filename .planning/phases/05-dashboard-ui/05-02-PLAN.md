---
phase: 05-dashboard-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/components/TopBar.tsx
  - packages/dashboard/src/components/DeviceSelector.tsx
  - packages/dashboard/src/components/ModuleShell.tsx
autonomous: true
requirements:
  - UI-01
  - UI-05
  - UI-07

must_haves:
  truths:
    - "Top bar renders with frosted glass background and shows simvyn branding on the left"
    - "Device selector dropdown has glass styling with smooth open/close and clear device status indicators"
    - "Connection status indicator uses a subtle animated pulse for connected state"
    - "ModuleShell empty state has a polished look consistent with Liquid Glass aesthetic"
    - "Loading skeleton uses glass styling"
  artifacts:
    - path: "packages/dashboard/src/components/TopBar.tsx"
      provides: "Refined top bar with glass styling, polished branding, connection pulse"
      contains: "backdrop"
    - path: "packages/dashboard/src/components/DeviceSelector.tsx"
      provides: "Enhanced device selector with glass dropdown, platform icons, status colors"
      contains: "glass"
    - path: "packages/dashboard/src/components/ModuleShell.tsx"
      provides: "Polished empty state and loading skeleton with glass aesthetic"
      contains: "glass-panel"
  key_links:
    - from: "packages/dashboard/src/components/TopBar.tsx"
      to: "packages/dashboard/src/hooks/use-ws.ts"
      via: "import useWs for connection status"
      pattern: "useWs"
---

<objective>
Polish the TopBar, DeviceSelector, and ModuleShell components to match the Liquid Glass aesthetic.

Purpose: These are the chrome components that frame every module panel. Polishing them elevates the entire dashboard experience without touching module-specific code.
Output: TopBar with refined glass styling and animated connection indicator. DeviceSelector with enhanced dropdown visuals. ModuleShell with polished empty and loading states.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-dashboard-ui/05-CONTEXT.md
@packages/dashboard/src/main.css
@packages/dashboard/src/components/TopBar.tsx
@packages/dashboard/src/components/DeviceSelector.tsx
@packages/dashboard/src/components/ModuleShell.tsx
@packages/dashboard/src/stores/device-store.ts
@packages/dashboard/src/hooks/use-ws.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine TopBar with Liquid Glass polish</name>
  <files>packages/dashboard/src/components/TopBar.tsx</files>
  <action>
Update `TopBar.tsx` with enhanced Liquid Glass styling:

- Keep the existing structure: left side = branding, right side = DeviceSelector + connection status
- **Branding**: Change from plain text to a slightly stylized "simvyn" — use `text-base font-semibold tracking-tight` with `text-accent-blue` gradient effect (use `bg-gradient-to-r from-accent-blue to-accent-purple bg-clip-text text-transparent` for a subtle gradient text)
- **Connection indicator**: Replace the static green/red dot with a pulsing indicator. Connected state: a small green dot with `animate-pulse` CSS animation (slow, 2s). Disconnected state: static red dot with no animation. Add a subtle `ring` effect around the dot when connected: `ring-2 ring-green-500/20`
- **Background**: Keep backdrop-blur and glass background. Increase the border-bottom opacity slightly to create a more defined separation. Use `border-b border-glass-border/60`
- **Height**: Increase from `h-14` to `h-12` (slightly more compact to give more space to content, matching the narrower Dock sidebar)
- Remove `bg-glass/80` and replace with a more explicit glass styling: `bg-[oklch(0.11_0.018_265_/_0.7)]` to match the dock sidebar's glass tint, with `backdrop-blur-2xl backdrop-saturate-[1.3]`
  </action>
  <verify>Confirm the TopBar renders with gradient text branding, pulsing connection indicator, and glass background.</verify>
  <done>TopBar has Liquid Glass styling with gradient branding and animated connection status.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance DeviceSelector with glass dropdown</name>
  <files>packages/dashboard/src/components/DeviceSelector.tsx</files>
  <action>
Enhance the DeviceSelector component:

- **Trigger button**: Use a more refined glass style. Replace the current `border border-border bg-bg-surface` with `bg-bg-surface/50 border border-glass-border hover:border-glass-border-hover hover:bg-glass-hover transition-all duration-150`. Add a subtle chevron animation — rotate the chevron SVG 180deg when open using `transition-transform duration-200` and `transform rotate-180` when `open` is true.
- **Dropdown panel**: The dropdown already uses `glass-panel` which is good. Enhance it:
  - Add a subtle shadow: `shadow-xl shadow-black/20`
  - Add animation: use a simple CSS transition — the dropdown should fade-in and scale from 95% to 100%. Apply `animate-in fade-in-0 zoom-in-95` or use inline styles with transition. Alternatively, add classes `opacity-100 scale-100` when visible and start from `opacity-0 scale-95` with `transition-all duration-150`.
  - For simplicity, just add `shadow-xl shadow-black/30` to the existing glass-panel dropdown.
- **Platform section headers**: Change emoji icons (🍎/🤖) to text labels only, since emojis don't match the Liquid Glass aesthetic. Just use "iOS" and "Android" as plain uppercase text labels.
- **State indicator**: Keep the green dot for booted, but add a subtle `ring` effect: booted gets `bg-green-500 ring-2 ring-green-500/20`, shutdown gets `bg-text-muted/50`
- **No other structural changes** — the existing click-outside behavior, broadcast mode, etc. all stay the same.
  </action>
  <verify>Open and close the device selector — confirm smooth visual transitions, platform labels (not emojis), and enhanced glass styling.</verify>
  <done>DeviceSelector has polished glass dropdown, refined trigger button, enhanced state indicators, no emojis.</done>
</task>

<task type="auto">
  <name>Task 3: Polish ModuleShell empty state and loading skeleton</name>
  <files>packages/dashboard/src/components/ModuleShell.tsx</files>
  <action>
Polish the ModuleShell component:

- **Empty state** (no module selected): Remove the puzzle emoji. Replace with a minimal text prompt. Use a subtle glass container:
  ```tsx
  <div className="flex flex-1 items-center justify-center">
    <div className="text-center">
      <p className="text-sm text-text-secondary">Select a module from the dock</p>
    </div>
  </div>
  ```
  
- **No panel available state** (module selected but no panel registered): Remove the construction emoji. Replace with:
  ```tsx
  <div className="flex h-full items-center justify-center">
    <div className="text-center">
      <p className="text-sm text-text-secondary">
        No panel available for <span className="font-medium text-text-primary">{mod.name}</span>
      </p>
    </div>
  </div>
  ```

- **Loading skeleton**: Enhance the skeleton to feel more glass-like. Change the skeleton from using a single `glass-panel` with `bg-bg-surface` blocks to using lighter glass-tinted shimmer blocks:
  ```tsx
  function LoadingSkeleton() {
    return (
      <div className="p-6 space-y-4">
        <div className="h-7 w-48 rounded-lg bg-bg-surface/60 animate-pulse" />
        <div className="glass-panel p-6 space-y-3">
          <div className="h-4 w-full rounded bg-bg-surface/40 animate-pulse" />
          <div className="h-4 w-3/4 rounded bg-bg-surface/40 animate-pulse" />
          <div className="h-4 w-1/2 rounded bg-bg-surface/40 animate-pulse" />
        </div>
      </div>
    );
  }
  ```

- No structural changes to the `display: none/block` module switching pattern — that stays exactly as is for state persistence (UI-07).
  </action>
  <verify>Confirm empty state shows clean text (no emojis), loading skeleton uses glass-tinted shimmer, and module switching still preserves state via display toggle.</verify>
  <done>ModuleShell has polished empty state (no emojis), glass-tinted loading skeleton, and module state persistence unchanged.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/dashboard` passes
2. TopBar renders with gradient text branding and pulsing green connection indicator
3. DeviceSelector dropdown has glass styling with shadow, no emojis for platform headers
4. DeviceSelector chevron rotates when dropdown is open
5. ModuleShell empty state shows clean text prompt without emojis
6. Loading skeleton uses glass-tinted shimmer blocks
7. Module state persistence (display:none/block) still works — switching modules and back preserves state
</verification>

<success_criteria>
TopBar, DeviceSelector, and ModuleShell are polished with Liquid Glass aesthetics. No emojis in any chrome component. Connection status has animated pulse. DeviceSelector dropdown is visually enhanced. Module state persistence is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-ui/05-02-SUMMARY.md`
</output>
