---
phase: 05-dashboard-ui
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/dashboard/src/panels/DevicePanel.tsx
  - packages/dashboard/src/panels/AppPanel.tsx
  - packages/dashboard/src/panels/LogPanel.tsx
  - packages/dashboard/src/panels/logs/LogToolbar.tsx
  - packages/dashboard/src/panels/logs/LogList.tsx
  - packages/dashboard/src/panels/apps/AppList.tsx
  - packages/dashboard/src/panels/apps/InstallDropZone.tsx
  - packages/dashboard/src/panels/apps/AppActions.tsx
  - packages/dashboard/src/panels/LocationPanel.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-08

must_haves:
  truths:
    - "All module panels use consistent glass panel styling from the design system"
    - "No emoji characters remain in any module panel (replaced with text or Lucide icons)"
    - "Panel headers have consistent typography (no h1 elements — use smaller, refined headings)"
    - "Device selector dropdowns within panels use consistent styling matching the TopBar device selector"
    - "All panels render properly at various screen widths without horizontal overflow"
    - "Button styles are consistent across all panels (same border-radius, sizing, color patterns)"
  artifacts:
    - path: "packages/dashboard/src/panels/DevicePanel.tsx"
      provides: "Polished device panel with no emojis, consistent button styles, responsive grid"
    - path: "packages/dashboard/src/panels/AppPanel.tsx"
      provides: "Polished app panel with consistent header and device selector styling"
    - path: "packages/dashboard/src/panels/LogPanel.tsx"
      provides: "Polished log panel with consistent header styling"
    - path: "packages/dashboard/src/panels/LocationPanel.tsx"
      provides: "Location panel with emoji-free toolbar"
  key_links:
    - from: "packages/dashboard/src/panels/DevicePanel.tsx"
      to: "packages/dashboard/src/stores/device-store.ts"
      via: "import useDeviceStore"
      pattern: "useDeviceStore"
---

<objective>
Polish all four module panels to match the Liquid Glass design system. Remove all emoji usage, enforce consistent typography and button patterns, and ensure responsive layouts.

Purpose: With the design system, sidebar, and chrome components polished (Plans 01-02), the module panels are the remaining pieces that need visual consistency. This plan brings all four panels up to the same standard without changing any functionality.
Output: All module panels visually unified with Liquid Glass aesthetic. No emojis. Consistent typography, button styles, and responsive behavior.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-dashboard-ui/05-CONTEXT.md
@packages/dashboard/src/main.css
@packages/dashboard/src/panels/DevicePanel.tsx
@packages/dashboard/src/panels/AppPanel.tsx
@packages/dashboard/src/panels/LogPanel.tsx
@packages/dashboard/src/panels/LocationPanel.tsx
@packages/dashboard/src/panels/logs/LogToolbar.tsx
@packages/dashboard/src/panels/logs/LogList.tsx
@packages/dashboard/src/panels/apps/AppList.tsx
@packages/dashboard/src/panels/apps/AppActions.tsx
@packages/dashboard/src/panels/apps/InstallDropZone.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish DevicePanel — remove emojis, refine styling</name>
  <files>packages/dashboard/src/panels/DevicePanel.tsx</files>
  <action>
Update DevicePanel with the following changes:

**Remove all emojis:**
- Empty state: Replace "📱" with text like "No Devices Detected" as a heading
- DeviceSection: Remove emoji `icon` parameter entirely — section headers should just be plain text "iOS Simulators" / "Android Emulators" with no icon
- Remove the `icon` prop from `DeviceSection` component

**Refine typography:**
- Main heading: Change from `text-xl font-semibold` to `text-base font-medium` — panels shouldn't scream, they should be understated
- Section headings: Keep `text-sm font-medium text-text-muted uppercase tracking-wider` but remove emoji prefix

**Enhance DeviceCard:**
- Keep glass-panel styling
- Add `hover:border-glass-border-hover transition-all duration-150` to the card for subtle hover state
- The truncated device ID should use `font-mono` for better readability

**Responsive grid:**
- Change grid from `grid-cols-1 md:grid-cols-2 xl:grid-cols-3` to `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` — devices panel may be narrower now with the 60px dock sidebar instead of 224px sidebar

**Button consistency:**
- ActionButton styles are already well-defined — keep as-is
- Refresh button: Ensure it matches the consistent button pattern used across all panels

**All `button` elements must have explicit `type="button"` prop.**
  </action>
  <verify>Device panel renders with no emojis, refined headers, hover effects on cards, and responsive grid. All buttons have type="button".</verify>
  <done>DevicePanel polished with no emojis, consistent typography, hover effects, responsive grid, and button type props.</done>
</task>

<task type="auto">
  <name>Task 2: Polish AppPanel, AppList, AppActions, InstallDropZone</name>
  <files>packages/dashboard/src/panels/AppPanel.tsx, packages/dashboard/src/panels/apps/AppList.tsx, packages/dashboard/src/panels/apps/AppActions.tsx, packages/dashboard/src/panels/apps/InstallDropZone.tsx</files>
  <action>
**AppPanel.tsx:**
- Heading: Change from `text-xl font-semibold` to `text-base font-medium` to match DevicePanel
- Device selector and Refresh button styling should be consistent with DevicePanel patterns

**AppList.tsx:**
- Table: Add `hover:bg-white/[0.03]` for slightly more visible hover rows (was `hover:bg-white/[0.02]`)
- Filter buttons are already well-styled — keep as-is

**AppActions.tsx:**
- Button styling is already consistent — keep as-is
- Ensure all buttons have `type="button"` (they already do)

**InstallDropZone.tsx:**
- Replace the border styling to use design system tokens: `border-glass-border` instead of `border-white/15`
- Drag-over state: `border-accent-blue/40 bg-accent-blue/[0.04]` instead of `border-accent-blue/60 bg-white/5`
- The "or click to browse" link should use `underline-offset-2` for a cleaner underline

No functional changes to any component.
  </action>
  <verify>App management panel renders with consistent heading size, refined drop zone borders, and consistent hover states. No emojis (there weren't any in app panel).</verify>
  <done>App management panel and sub-components polished with consistent styling.</done>
</task>

<task type="auto">
  <name>Task 3: Polish LogPanel, LogToolbar, LogList</name>
  <files>packages/dashboard/src/panels/LogPanel.tsx, packages/dashboard/src/panels/logs/LogToolbar.tsx, packages/dashboard/src/panels/logs/LogList.tsx</files>
  <action>
**LogPanel.tsx:**
- Heading: Change from `text-xl font-semibold` to `text-base font-medium`
- Empty state: Remove glass-panel wrapper and use simpler text like the ModuleShell empty state pattern

**LogToolbar.tsx:**
- Level filter buttons: Keep the existing color-coded active states — they're well done
- Inputs: Ensure consistent styling with other panels' inputs. The current styling is already good.
- Export and Clear buttons: Ensure consistent sizing and hover states

**LogList.tsx:**
- The monospace log display is already clean
- Hover row: Keep `hover:bg-white/[0.02]` — logs need very subtle hover since there are many rows
- The glass-panel container for the log list is good — keep as-is

No functional changes.
  </action>
  <verify>Log panel renders with consistent heading size and refined empty state.</verify>
  <done>Log panel and sub-components polished with consistent typography.</done>
</task>

<task type="auto">
  <name>Task 4: Polish LocationPanel toolbar</name>
  <files>packages/dashboard/src/panels/LocationPanel.tsx</files>
  <action>
**LocationPanel.tsx:**
- The location panel has no heading (it's map-first with a toolbar) — this is fine, no change needed
- The toolbar uses CSS classes from location-panel.css which already have glass styling — keep as-is
- "Favs" button text: Change to a Lucide icon (Star or Heart) for consistency with the icon-forward design. Import from lucide-react:
  ```tsx
  import { Star } from "lucide-react";
  ```
  Replace the "Favs" text button with:
  ```tsx
  <button ...>
    <Star size={14} strokeWidth={1.8} />
  </button>
  ```
  Keep the same class names and toggle behavior.
- Device selector in the toolbar: Keep as-is (it's a small select element that fits the compact toolbar layout)

No functional changes. Location panel is already the most visually polished panel due to the map taking up most space.
  </action>
  <verify>Location panel toolbar shows a Star icon instead of "Favs" text. Map and playback controls render unchanged.</verify>
  <done>Location panel toolbar refined with Star icon for favorites toggle.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/dashboard` passes
2. No emoji characters appear anywhere in the rendered dashboard (search codebase for emoji unicode ranges)
3. All panel headings use consistent `text-base font-medium` typography
4. Device cards have hover border effect
5. DevicePanel grid is responsive at various widths
6. InstallDropZone uses design system border tokens
7. Location panel shows Star icon for favorites
8. All buttons across all panels have explicit `type="button"` props
</verification>

<success_criteria>
All four module panels visually match the Liquid Glass design system. No emojis remain. Typography, buttons, hover states, and responsive layouts are consistent across all panels.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-ui/05-04-SUMMARY.md`
</output>
