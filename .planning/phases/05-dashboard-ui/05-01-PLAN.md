---
phase: 05-dashboard-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/package.json
  - packages/dashboard/src/main.css
  - packages/dashboard/index.html
  - packages/dashboard/src/components/Sidebar.tsx
  - packages/dashboard/src/App.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04
  - UI-06

must_haves:
  truths:
    - "Background renders a dark gradient (not flat color) with deep blue-purple tones"
    - "Glass panels use backdrop-filter: blur + saturate with visible frosted glass effect"
    - "Sidebar is a narrow vertical dock pinned to the left edge with icons only (no text labels)"
    - "Sidebar icons show tooltip with module name on hover"
    - "Active module icon has a glowing dot indicator below it (like macOS Dock running app indicator)"
    - "Hovered icon scales up subtly (~1.15x) with CSS transition"
    - "Inter font is loaded and applied as primary font"
    - "Custom thin translucent scrollbar styles are applied globally"
  artifacts:
    - path: "packages/dashboard/src/main.css"
      provides: "Liquid Glass design system tokens, gradient background, scrollbar styles, glass-panel refinements"
      contains: "scrollbar"
    - path: "packages/dashboard/src/components/Sidebar.tsx"
      provides: "macOS Dock-style icon sidebar with Lucide icons, hover scale, active dot indicator, tooltips"
      contains: "lucide-react"
    - path: "packages/dashboard/index.html"
      provides: "Inter font preconnect/link"
      contains: "Inter"
  key_links:
    - from: "packages/dashboard/src/components/Sidebar.tsx"
      to: "packages/dashboard/src/stores/module-store.ts"
      via: "import useModuleStore"
      pattern: "useModuleStore"
---

<objective>
Establish the Liquid Glass design system foundation and transform the sidebar into a macOS Dock-style icon bar.

Purpose: This plan lays the visual groundwork for the entire dashboard. The background gradient, glass-panel refinements, font loading, scrollbar styling, and Dock sidebar are the most visible changes that define the aesthetic. Every subsequent plan builds on these design tokens and patterns.
Output: Dark gradient background, refined glass-panel tokens, Inter font loaded, custom scrollbars, and a Dock-style icon sidebar with Lucide icons, hover magnification, active dot indicator, and tooltips.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-ui/05-CONTEXT.md
@packages/dashboard/src/main.css
@packages/dashboard/src/main.tsx
@packages/dashboard/src/App.tsx
@packages/dashboard/src/components/Sidebar.tsx
@packages/dashboard/src/components/TopBar.tsx
@packages/dashboard/src/components/ModuleShell.tsx
@packages/dashboard/src/stores/module-store.ts
@packages/dashboard/index.html
@packages/dashboard/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Lucide React and load Inter font</name>
  <files>packages/dashboard/package.json, packages/dashboard/index.html</files>
  <action>
Install `lucide-react` as a dependency in the dashboard package:

```bash
npm install lucide-react --workspace=@simvyn/dashboard
```

In `packages/dashboard/index.html`, add Inter font loading via Google Fonts in the `<head>`:

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet" />
```
  </action>
  <verify>Run `ls packages/dashboard/node_modules/lucide-react` to confirm installation. Open index.html to confirm font links are present.</verify>
  <done>lucide-react installed and Inter font loading via Google Fonts in index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul main.css with Liquid Glass design system</name>
  <files>packages/dashboard/src/main.css</files>
  <action>
Replace the entire `main.css` with an expanded Liquid Glass design system:

```css
@import "tailwindcss";

@theme {
  --color-bg-base: oklch(0.08 0.015 260);
  --color-bg-surface: oklch(0.12 0.015 260);
  --color-bg-elevated: oklch(0.16 0.018 260);
  --color-glass: oklch(0.15 0.02 260 / 0.6);
  --color-glass-hover: oklch(0.18 0.022 260 / 0.7);
  --color-glass-border: oklch(0.35 0.02 260 / 0.15);
  --color-glass-border-hover: oklch(0.4 0.025 260 / 0.25);
  --color-accent-blue: oklch(0.7 0.12 240);
  --color-accent-purple: oklch(0.65 0.12 290);
  --color-accent-teal: oklch(0.72 0.1 190);
  --color-text-primary: oklch(0.92 0.01 260);
  --color-text-secondary: oklch(0.65 0.01 260);
  --color-text-muted: oklch(0.45 0.01 260);
  --color-border: oklch(0.25 0.02 260 / 0.5);
  --radius-panel: 14px;
  --radius-button: 10px;
}

html {
  height: 100%;
}

body {
  height: 100%;
  background: linear-gradient(
    145deg,
    oklch(0.06 0.025 280) 0%,
    oklch(0.08 0.02 260) 30%,
    oklch(0.07 0.018 240) 60%,
    oklch(0.09 0.025 270) 100%
  );
  color: var(--color-text-primary);
  font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
  font-feature-settings: "cv11", "ss01";
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Glass panel base */
.glass-panel {
  backdrop-filter: blur(24px) saturate(1.3);
  background: var(--color-glass);
  border: 1px solid var(--color-glass-border);
  border-radius: var(--radius-panel);
}

/* Thin translucent scrollbars */
::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: oklch(0.4 0.015 260 / 0.3);
  border-radius: 3px;
}

::-webkit-scrollbar-thumb:hover {
  background: oklch(0.5 0.015 260 / 0.45);
}

::-webkit-scrollbar-corner {
  background: transparent;
}

/* Firefox scrollbar */
* {
  scrollbar-width: thin;
  scrollbar-color: oklch(0.4 0.015 260 / 0.3) transparent;
}

/* Dock sidebar */
.dock-sidebar {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 12px 0;
  gap: 4px;
  width: 60px;
  backdrop-filter: blur(24px) saturate(1.3);
  background: oklch(0.11 0.018 265 / 0.7);
  border-right: 1px solid var(--color-glass-border);
}

.dock-icon {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 42px;
  height: 42px;
  border-radius: 12px;
  color: var(--color-text-secondary);
  cursor: pointer;
  transition: transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1),
              background 0.15s ease,
              color 0.15s ease;
  user-select: none;
}

.dock-icon:hover {
  transform: scale(1.15);
  background: oklch(0.2 0.02 260 / 0.5);
  color: var(--color-text-primary);
}

.dock-icon.active {
  color: var(--color-accent-blue);
  background: oklch(0.2 0.03 240 / 0.3);
}

/* Active dot indicator (macOS Dock style) */
.dock-icon.active::after {
  content: "";
  position: absolute;
  bottom: -2px;
  left: 50%;
  transform: translateX(-50%);
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: var(--color-accent-blue);
  box-shadow: 0 0 6px var(--color-accent-blue);
}

/* Tooltip on hover */
.dock-icon .dock-tooltip {
  position: absolute;
  left: 100%;
  top: 50%;
  transform: translateY(-50%);
  margin-left: 8px;
  padding: 4px 10px;
  border-radius: 6px;
  backdrop-filter: blur(20px) saturate(1.2);
  background: oklch(0.15 0.02 260 / 0.9);
  border: 1px solid var(--color-glass-border);
  font-size: 12px;
  font-weight: 500;
  color: var(--color-text-primary);
  white-space: nowrap;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.15s ease;
  z-index: 100;
}

.dock-icon:hover .dock-tooltip {
  opacity: 1;
}
```

Key changes from current:
- Added `--color-bg-elevated` and `--color-glass-hover`/`--color-glass-border-hover` tokens
- Body background changed from flat `bg-base` to a 4-stop diagonal gradient
- Glass panel blur increased from 20px to 24px, saturation from 1.2 to 1.3
- Added `font-feature-settings` for OpenType features
- Added `-webkit-font-smoothing` for crisp text
- Custom scrollbar styles for WebKit and Firefox
- Dock sidebar CSS classes (`.dock-sidebar`, `.dock-icon`, `.dock-tooltip`, active dot)
  </action>
  <verify>Visually inspect that the CSS is well-formed. Confirm no existing utility classes are broken by the token changes.</verify>
  <done>Liquid Glass design system tokens expanded, gradient background applied, scrollbar styles added, dock sidebar CSS classes defined.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild Sidebar as macOS Dock with Lucide icons</name>
  <files>packages/dashboard/src/components/Sidebar.tsx, packages/dashboard/src/App.tsx</files>
  <action>
Replace the entire `Sidebar.tsx` with a macOS Dock-style icon bar:

```tsx
import { MapPin, AppWindow, ScrollText, MonitorSmartphone } from "lucide-react";
import type { ComponentType } from "react";
import { useModuleStore } from "../stores/module-store";

const iconMap: Record<string, ComponentType<{ size?: number; strokeWidth?: number }>> = {
  devices: MonitorSmartphone,
  location: MapPin,
  apps: AppWindow,
  logs: ScrollText,
};

const labelMap: Record<string, string> = {
  devices: "Devices",
  location: "Location",
  apps: "Apps",
  logs: "Logs",
};

export default function Sidebar() {
  const { modules, activeModule, setActiveModule } = useModuleStore();

  return (
    <aside className="dock-sidebar">
      {modules.map((mod) => {
        const isActive = activeModule === mod.name;
        const Icon = iconMap[mod.name];
        const label = labelMap[mod.name] ?? mod.name;

        return (
          <button
            key={mod.name}
            type="button"
            onClick={() => setActiveModule(mod.name)}
            className={`dock-icon ${isActive ? "active" : ""}`}
          >
            {Icon ? <Icon size={20} strokeWidth={1.8} /> : <span className="text-sm">{mod.name[0]?.toUpperCase()}</span>}
            <span className="dock-tooltip">{label}</span>
          </button>
        );
      })}

      {modules.length === 0 && (
        <div className="text-text-muted text-[10px] text-center px-1 py-4">
          No modules
        </div>
      )}
    </aside>
  );
}
```

Key changes:
- Replaced text sidebar (w-56) with narrow icon dock (w-60 via CSS)
- Emoji icons replaced with Lucide icons (MonitorSmartphone, MapPin, AppWindow, ScrollText)
- Text labels replaced with hover tooltips
- Active state uses glowing dot indicator (via `.dock-icon.active::after` in CSS)
- Hover state uses subtle scale-up (via `.dock-icon:hover` in CSS)
- `iconMap` and `labelMap` are separate objects, making icon swapping easy in the future (when custom SVGs replace Lucide)

In `App.tsx`, no structural changes needed — the Sidebar component is already used in the layout. The `w-56` was on the sidebar's `aside` element itself, now replaced by the `dock-sidebar` CSS class with `width: 60px`.
  </action>
  <verify>Run `npx tsc --noEmit -p packages/dashboard` — should compile with no errors. Visually confirm the sidebar renders as a narrow icon dock.</verify>
  <done>Sidebar is a macOS Dock-style icon bar with Lucide icons, hover scale, active dot indicator, and hover tooltips. Module icon system is designed for easy future icon swapping.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/dashboard` passes
2. `lucide-react` is listed in dashboard's package.json dependencies
3. Inter font is loaded via Google Fonts link in index.html
4. Background renders as a diagonal gradient (not flat color)
5. Glass panels have enhanced blur/saturation
6. Sidebar renders as a narrow ~60px icon dock with Lucide icons
7. Hovering an icon shows a tooltip with module name and scales the icon up
8. Active module icon has a glowing blue dot below it
9. Custom thin translucent scrollbars appear on scrollable elements
</verification>

<success_criteria>
Liquid Glass design system tokens are expanded. Background is a deep blue-purple gradient. Sidebar is a macOS Dock-style icon bar with Lucide icons, hover tooltips, scale-up, and active dot indicator. Inter font is loaded. Custom scrollbar styles are applied globally.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-ui/05-01-SUMMARY.md`
</output>
