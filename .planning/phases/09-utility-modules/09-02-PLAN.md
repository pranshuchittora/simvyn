---
phase: 09-utility-modules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/device.ts
  - packages/core/src/adapters/ios.ts
  - packages/core/src/adapters/android.ts
  - packages/modules/media/package.json
  - packages/modules/media/tsconfig.json
  - packages/modules/media/manifest.ts
  - packages/modules/media/routes.ts
  - packages/modules/media/cli.ts
  - package.json
autonomous: true
requirements: [MED-01, MED-02, MED-03, MED-04]

must_haves:
  truths:
    - "User can push a photo or video to an iOS simulator camera roll via simctl addmedia"
    - "User can push a photo or video to an Android device via adb push + media scanner broadcast"
    - "REST API accepts multipart file upload and injects media into the device"
    - "CLI command simvyn media add <device> <file> injects a local file into the device"
  artifacts:
    - path: "packages/modules/media/manifest.ts"
      provides: "Module registration with routes and CLI"
      exports: ["default"]
    - path: "packages/modules/media/routes.ts"
      provides: "REST API endpoint with multipart file upload for media injection"
      exports: ["mediaRoutes"]
    - path: "packages/modules/media/cli.ts"
      provides: "CLI subcommand for media injection"
      exports: ["registerMediaCli"]
    - path: "packages/core/src/adapters/ios.ts"
      provides: "addMedia method using xcrun simctl addmedia"
      contains: "addMedia"
    - path: "packages/core/src/adapters/android.ts"
      provides: "addMedia method using adb push + media scanner broadcast"
      contains: "addMedia"
  key_links:
    - from: "packages/modules/media/routes.ts"
      to: "packages/core/src/adapters/ios.ts"
      via: "fastify.deviceManager.getAdapter(device.platform)"
      pattern: "adapter\\?\\.addMedia"
    - from: "packages/modules/media/routes.ts"
      to: "packages/core/src/adapters/android.ts"
      via: "fastify.deviceManager.getAdapter(device.platform)"
      pattern: "adapter\\?\\.addMedia"
---

<objective>
Build the media injection module — push photos and videos to iOS simulator camera rolls and Android device galleries via adapter methods, REST API with file upload, and CLI.

Purpose: Enable developers to inject test media (screenshots, photos, videos) into devices without manual file management.
Output: Working @simvyn/module-media package with multipart upload route and CLI subcommand.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/types/src/device.ts
@packages/core/src/adapters/ios.ts
@packages/core/src/adapters/android.ts
@packages/modules/app-management/routes.ts
@packages/modules/settings/manifest.ts
@packages/modules/settings/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add addMedia adapter methods and create media module with routes and CLI</name>
  <files>
    packages/types/src/device.ts
    packages/core/src/adapters/ios.ts
    packages/core/src/adapters/android.ts
    packages/modules/media/package.json
    packages/modules/media/tsconfig.json
    packages/modules/media/manifest.ts
    packages/modules/media/routes.ts
    packages/modules/media/cli.ts
    package.json
  </files>
  <action>
1. In `packages/types/src/device.ts`:
   - Add `addMedia?(deviceId: string, filePath: string): Promise<void>;` to the `PlatformAdapter` interface
   - The `"addMedia"` capability already exists in PlatformCapability — no change needed there

2. In `packages/core/src/adapters/ios.ts`:
   - Add `addMedia` method: `xcrun simctl addmedia <deviceId> <filePath>`
   - Uses `execFileAsync("xcrun", ["simctl", "addmedia", deviceId, filePath])`
   - The capability `"addMedia"` is already in the iOS capabilities array — no change needed

3. In `packages/core/src/adapters/android.ts`:
   - Add `addMedia` method:
     - Extract basename from filePath: `const filename = path.basename(filePath)`
     - Push file to device: `adb -s <deviceId> push <filePath> /sdcard/DCIM/<filename>`
     - Trigger media scanner: `adb -s <deviceId> shell am broadcast -a android.intent.action.MEDIA_SCANNER_SCAN_FILE -d file:///sdcard/DCIM/<filename>`
     - Uses `execFileAsync` for both commands
   - Guard against `avd:` prefix device IDs (same pattern as existing methods)
   - The capability `"addMedia"` is already in the Android capabilities array — no change needed

4. Create `packages/modules/media/package.json`:
   - name: `@simvyn/module-media`, version: `0.1.0`
   - type: `module`, main: `./manifest.ts`
   - Dependencies: `@simvyn/types: "*"`, `@simvyn/core: "*"`, `@simvyn/server: "*"`

5. Create `packages/modules/media/tsconfig.json`:
   - Extend `../../../tsconfig.json`
   - References to `../../types`, `../../core`, `../../server`
   - compilerOptions: module NodeNext, moduleResolution NodeNext

6. Add `"packages/modules/media"` to root `package.json` workspaces array

7. Create `packages/modules/media/routes.ts`:
   Export `async function mediaRoutes(fastify: FastifyInstance)`:
   - Register `@fastify/multipart` with `{ limits: { fileSize: 500_000_000 } }` (same as app-management)
   
   **POST /add/:deviceId** — Upload and inject media file
   - Parse multipart file from request using `req.file()`
   - Save to temp directory: `mkdtemp(join(tmpdir(), 'simvyn-media-'))`, then write file using pipeline + createWriteStream
   - Check device exists and is booted (404/400 pattern)
   - Get adapter, check `adapter.addMedia` exists (400 if not supported)
   - Call `adapter.addMedia(device.id, tempFilePath)`
   - Clean up temp directory with `rm(tempDir, { recursive: true })`
   - Return `{ success: true, filename: originalFilename }`
   - Wrap in try/catch, clean up temp on error too

   Follow the exact same multipart handling pattern as `packages/modules/app-management/routes.ts` (file save to temp → operation → cleanup).

8. Create `packages/modules/media/cli.ts`:
   Export `function registerMediaCli(program: Command)`:

   **`simvyn media add <device> <file>`**
   - Verify file exists using `fs.stat` (exit 1 with "File not found" if missing)
   - Lazy-import `@simvyn/core`
   - Create adapters + device manager, refresh, find device by ID/prefix
   - Check device is booted
   - Get adapter, check `adapter.addMedia` exists
   - Call `adapter.addMedia(target.id, absoluteFilePath)` — resolve file path with `path.resolve`
   - Print `Added <basename> to <device.name>`
   - Always call `dm.stop()` in finally block

9. Create `packages/modules/media/manifest.ts`:
   - Default export a `SimvynModule` with name: `"media"`, version: `"0.1.0"`
   - register() registers mediaRoutes
   - cli() delegates to registerMediaCli
   - capabilities: `["addMedia"]`
  </action>
  <verify>`npm install` succeeds, then `npx tsc --noEmit -p packages/core/tsconfig.json && npx tsc --noEmit -p packages/modules/media/tsconfig.json`</verify>
  <done>addMedia adapter methods implemented for iOS (simctl addmedia) and Android (adb push + media scanner), REST API accepts multipart upload, CLI `simvyn media add` works headlessly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/types/tsconfig.json` passes
- `npx tsc --noEmit -p packages/core/tsconfig.json` passes
- `npx tsc --noEmit -p packages/modules/media/tsconfig.json` passes
- `npm install` succeeds with new workspace
- Module auto-discovery picks up media module
</verification>

<success_criteria>
- PlatformAdapter has addMedia optional method
- iOS adapter uses xcrun simctl addmedia
- Android adapter uses adb push to /sdcard/DCIM/ + media scanner broadcast
- REST API has POST /add/:deviceId accepting multipart file upload
- CLI `simvyn media add <device> <file>` validates file exists and injects into device
- Everything compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-utility-modules/09-02-SUMMARY.md`
</output>
