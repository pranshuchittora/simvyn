---
phase: 17.1-typography-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/index.html
  - packages/dashboard/src/main.css
  - packages/dashboard/src/components/TopBar.tsx
  - packages/dashboard/src/components/HomeScreen.tsx
autonomous: true
requirements: [TYPO-01, TYPO-02]

must_haves:
  truths:
    - "TopBar 'simvyn' text renders in Cascadia Code font, white color (not gradient)"
    - "HomeScreen 'simvyn' heading renders in Cascadia Code font"
    - "Cascadia Code font loads without FOUT or layout shift"
  artifacts:
    - path: "packages/dashboard/index.html"
      provides: "Google Fonts link for Cascadia Code"
    - path: "packages/dashboard/src/main.css"
      provides: "Brand font CSS variable or utility class"
    - path: "packages/dashboard/src/components/TopBar.tsx"
      provides: "White Cascadia Code simvyn text"
    - path: "packages/dashboard/src/components/HomeScreen.tsx"
      provides: "Cascadia Code simvyn heading"
  key_links:
    - from: "packages/dashboard/index.html"
      to: "packages/dashboard/src/main.css"
      via: "Google Fonts CSS loaded before app renders"
      pattern: "Cascadia"
    - from: "packages/dashboard/src/components/TopBar.tsx"
      to: "packages/dashboard/src/main.css"
      via: "font-family class or variable"
      pattern: "cascadia|brand"
---

<objective>
Add Cascadia Code as the branding font for simvyn identity and apply it to all "simvyn" text in the dashboard.

Purpose: Establish a distinctive typographic identity — Cascadia Code for the "simvyn" brand name, white in the TopBar for clean readability against the glass header.

Output: Cascadia Code loaded, TopBar "simvyn" white with branding font, HomeScreen "simvyn" with branding font.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/dashboard/index.html
@packages/dashboard/src/main.css
@packages/dashboard/src/components/TopBar.tsx
@packages/dashboard/src/components/HomeScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cascadia Code font and branding CSS</name>
  <files>packages/dashboard/index.html, packages/dashboard/src/main.css</files>
  <action>
1. **index.html**: Add Cascadia Code to the existing Google Fonts link. The current link loads Inter. Add Cascadia Code alongside it:
   - Change the Google Fonts URL to include both families: `family=Cascadia+Code:wght@400;600&family=Inter:wght@300;400;500;600`
   - If Cascadia Code is not available on Google Fonts, use fontsource CDN instead: add a `<link>` for `https://cdn.jsdelivr.net/fontsource/fonts/cascadia-code@latest/latin-400-normal.woff2` or install `@fontsource/cascadia-code` as a dependency and import in main.tsx. Prefer the CDN approach to match the existing Inter loading pattern.

2. **main.css**: Add a `--font-brand` CSS custom property in the `@theme` block:
   ```
   --font-brand: "Cascadia Code", "SF Mono", ui-monospace, monospace;
   ```
   This gives a branded monospace fallback chain.

Note: Do NOT change the base body font (Inter) — Cascadia Code is only for branding text where "simvyn" appears.
  </action>
  <verify>Run `npm run build --workspace=packages/dashboard` — build succeeds. Inspect index.html has the font link. Grep main.css for `--font-brand` or `Cascadia Code`.</verify>
  <done>Cascadia Code font is loaded via CDN and a `--font-brand` variable exists in main.css for use by components.</done>
</task>

<task type="auto">
  <name>Task 2: Apply branding font and white color to simvyn text</name>
  <files>packages/dashboard/src/components/TopBar.tsx, packages/dashboard/src/components/HomeScreen.tsx</files>
  <action>
1. **TopBar.tsx** (line 24): Replace the gradient text classes on the "simvyn" `<span>`:
   - Current: `text-base font-semibold tracking-tight bg-gradient-to-r from-accent-blue to-accent-purple bg-clip-text text-transparent`
   - New: `text-base font-semibold tracking-tight text-white` plus `style={{ fontFamily: 'var(--font-brand)' }}`
   - Alternatively, use a Tailwind arbitrary value: `font-[family-name:var(--font-brand)]` if supported by the Tailwind v4 setup. If not, inline style is fine — it's a one-off branding element.

2. **HomeScreen.tsx** (line 39): Add the branding font to the h1:
   - Current: `text-4xl font-semibold text-text-primary tracking-wide`
   - Add branding font the same way: `style={{ fontFamily: 'var(--font-brand)' }}` or Tailwind arbitrary value
   - Keep the existing text-text-primary color (white-ish) — only TopBar needs explicit `text-white`

Per user decision: TopBar simvyn = white + Cascadia Code. HomeScreen simvyn = Cascadia Code (keep existing color).
  </action>
  <verify>Run `npm run build --workspace=packages/dashboard` — build succeeds. Start dev server and visually confirm: TopBar shows white "simvyn" in Cascadia Code, HomeScreen shows "simvyn" in Cascadia Code.</verify>
  <done>TopBar "simvyn" is white with Cascadia Code font. HomeScreen "simvyn" heading uses Cascadia Code font. No other text affected.</done>
</task>

</tasks>

<verification>
- Cascadia Code font loads (check Network tab for font request)
- TopBar "simvyn" text is white (not gradient) and uses Cascadia Code
- HomeScreen "simvyn" heading uses Cascadia Code
- `npm run build --workspace=packages/dashboard` passes
- No other text in the dashboard is affected (body font still Inter)
</verification>

<success_criteria>
The "simvyn" brand name in the dashboard uses Cascadia Code font. TopBar text is white for clean readability. HomeScreen text uses the branding font. The rest of the dashboard typography is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-typography-update/17.1-01-SUMMARY.md`
</output>
