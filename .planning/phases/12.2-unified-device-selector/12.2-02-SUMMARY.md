---
phase: 12.2-unified-device-selector
plan: 02
subsystem: ui
tags: [zustand, react, device-selector, multi-select, panels]

requires:
  - phase: 12.2-unified-device-selector
    provides: selectedDeviceIds array and adaptive DeviceSelector component
provides:
  - "All 12 panels consume device selection from global store"
  - "Zero per-panel device selectors remain"
  - "LocationPanel multi-device support for set-location and start-playback"
affects: []

tech-stack:
  added: []
  patterns: ["useDeviceStore((s) => s.selectedDeviceIds[0] ?? null) for single-select panels"]

key-files:
  created: []
  modified:
    - packages/dashboard/src/panels/LogPanel.tsx
    - packages/dashboard/src/panels/AppPanel.tsx
    - packages/dashboard/src/panels/ScreenshotPanel.tsx
    - packages/dashboard/src/panels/PushPanel.tsx
    - packages/dashboard/src/panels/DeepLinksPanel.tsx
    - packages/dashboard/src/panels/SettingsPanel.tsx
    - packages/dashboard/src/panels/CrashLogsPanel.tsx
    - packages/dashboard/src/panels/MediaPanel.tsx
    - packages/dashboard/src/panels/ClipboardPanel.tsx
    - packages/dashboard/src/panels/DatabasePanel.tsx
    - packages/dashboard/src/panels/FileSystemPanel.tsx
    - packages/dashboard/src/panels/LocationPanel.tsx
    - packages/dashboard/src/panels/location/PlaybackControls.tsx

key-decisions:
  - "DatabasePanel/FileSystemPanel use useRef-based prev device tracking to reset app selection on device change"
  - "LocationPanel iterates selectedDeviceIds for multi-device set-location and start-playback"

patterns-established:
  - "Global device store read pattern: useDeviceStore((s) => s.selectedDeviceIds[0] ?? null)"
  - "App-dependent panels keep app <select> but remove device <select>"

requirements-completed: [DEVSEL-01, DEVSEL-03]

duration: 5min
completed: 2026-02-26
---

# Phase 12.2 Plan 02: Panel Wiring to Global Device Store Summary

**Removed all 12 per-panel device selectors and wired panels to global useDeviceStore, with LocationPanel multi-device iteration for set-location and route playback**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-26T20:31:54Z
- **Completed:** 2026-02-26T20:37:22Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments
- Removed device `<select>` dropdowns and local `useState`/auto-select `useEffect` from all 12 panel files
- All single-select panels now read `selectedDeviceIds[0]` from the global zustand store
- DatabasePanel and FileSystemPanel kept their app selectors while removing device selectors
- LocationPanel sends set-location and start-playback to all selected devices (multi-device)

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove device selectors from 9 single-select panels** - `915d8a2` (feat)
2. **Task 2: Wire DatabasePanel, FileSystemPanel, LocationPanel to global store** - `1fc24dd` (feat)
3. **Deviation fix: Update PlaybackControls for multi-device** - `a6cba77` (fix)

## Files Created/Modified
- `packages/dashboard/src/panels/LogPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/AppPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/ScreenshotPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/PushPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/DeepLinksPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/SettingsPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/CrashLogsPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/MediaPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/ClipboardPanel.tsx` - Reads selectedDeviceIds[0] from store, removed local state
- `packages/dashboard/src/panels/DatabasePanel.tsx` - Device from store, app selector kept, useRef device change detection
- `packages/dashboard/src/panels/FileSystemPanel.tsx` - Device from store, app selector kept, useRef device change detection
- `packages/dashboard/src/panels/LocationPanel.tsx` - Multi-device iteration for set-location and start-playback
- `packages/dashboard/src/panels/location/PlaybackControls.tsx` - Multi-device iteration for play, pause, stop, set-speed

## Decisions Made
- DatabasePanel/FileSystemPanel use `useRef` to track previous device and reset app selection on change (replaces the old `handleDeviceChange` callback that set both at once)
- LocationPanel iterates `selectedDeviceIds` array and sends individual WS messages per device (no server changes needed)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed PlaybackControls referencing removed selectedDeviceId property**
- **Found during:** Task 2 (LocationPanel wiring)
- **Issue:** PlaybackControls.tsx still referenced `useDeviceStore.getState().selectedDeviceId` (singular) which no longer exists after Plan 01's store migration to `selectedDeviceIds`
- **Fix:** Updated all 6 references to iterate `selectedDeviceIds` array, sending playback commands to all selected devices
- **Files modified:** packages/dashboard/src/panels/location/PlaybackControls.tsx
- **Verification:** `npm run build` passes with zero TypeScript errors
- **Committed in:** `a6cba77`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Essential fix — PlaybackControls would have had TypeScript errors without this update. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Phase 12.2 complete (2/2 plans). All panels consume device selection from the unified TopBar DeviceSelector. Ready for phase transition.

## Self-Check: PASSED

---
*Phase: 12.2-unified-device-selector*
*Completed: 2026-02-26*
