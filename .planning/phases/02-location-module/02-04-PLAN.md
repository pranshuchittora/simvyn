---
phase: 02-location-module
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - packages/dashboard/package.json
  - packages/dashboard/src/App.tsx
  - packages/dashboard/src/panels/LocationPanel.tsx
  - packages/dashboard/src/panels/location/MapView.tsx
  - packages/dashboard/src/panels/location/SearchBar.tsx
  - packages/dashboard/src/panels/location/PlaybackControls.tsx
  - packages/dashboard/src/panels/location/FavoritesPanel.tsx
  - packages/dashboard/src/panels/location/RouteLayer.tsx
  - packages/dashboard/src/panels/location/LocationMarker.tsx
  - packages/dashboard/src/panels/location/PlaybackMarker.tsx
  - packages/dashboard/src/panels/location/ModeSelector.tsx
  - packages/dashboard/src/panels/location/FileImportButton.tsx
  - packages/dashboard/src/panels/location/markers.ts
  - packages/dashboard/src/panels/location/location-panel.css
  - packages/dashboard/src/panels/location/stores/location-store.ts
  - packages/dashboard/src/panels/location/stores/playback-store.ts
  - packages/dashboard/src/panels/location/stores/route-store.ts
  - packages/dashboard/src/panels/location/stores/favorites-store.ts
autonomous: false
requirements:
  - LOC-07
  - LOC-09

must_haves:
  truths:
    - "Dashboard shows a 'location' module in the sidebar that renders an interactive Leaflet map when clicked"
    - "User can click on the map to place a marker and set GPS coordinates on the selected device"
    - "User can search for a location by name via the search bar, see results, and pick one to set on the map"
    - "User can import a GPX/KML file, see the route drawn on the map, and control playback with play/pause/stop"
    - "Playback marker moves along the route in real-time as WS broadcasts arrive"
    - "User can save locations and routes as favorites that persist across sessions"
    - "Mode toggle switches between Point mode (click to set) and Route mode (click to add waypoints)"
  artifacts:
    - path: "packages/dashboard/src/panels/LocationPanel.tsx"
      provides: "Main location panel component with registerPanel side-effect"
      contains: "registerPanel.*location"
    - path: "packages/dashboard/src/panels/location/MapView.tsx"
      provides: "Leaflet map container filling the panel"
      contains: "MapContainer"
    - path: "packages/dashboard/src/panels/location/stores/location-store.ts"
      provides: "Zustand store for current location state"
      contains: "create.*useLocationStore"
    - path: "packages/dashboard/src/panels/location/stores/playback-store.ts"
      provides: "Zustand store for playback state"
      contains: "create.*usePlaybackStore"
  key_links:
    - from: "packages/dashboard/src/App.tsx"
      to: "packages/dashboard/src/panels/LocationPanel.tsx"
      via: "side-effect import for panel registration"
      pattern: "import.*panels/LocationPanel"
    - from: "packages/dashboard/src/panels/LocationPanel.tsx"
      to: "packages/dashboard/src/stores/panel-registry.ts"
      via: "registerPanel('location', LocationPanel)"
      pattern: "registerPanel.*location"
    - from: "packages/dashboard/src/panels/location/MapView.tsx"
      to: "/api/modules/location/set"
      via: "fetch POST on map click to set location"
      pattern: "fetch.*location/set"
    - from: "packages/dashboard/src/panels/location/stores/playback-store.ts"
      to: "WS channel location"
      via: "useWsListener for playback-position and playback-complete"
      pattern: "useWsListener.*location"
---

<objective>
Build the dashboard location panel: interactive Leaflet map with search, point/route modes, playback controls, and favorites management.

Purpose: The map UI is the primary way users interact with the location module. It connects to the server routes (Plan 02) and WS channel for real-time playback visualization.

Output: Complete LocationPanel registered in the dashboard with all sub-components, zustand stores, CSS, and WS integration.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-location-module/02-RESEARCH.md
@.planning/phases/02-location-module/02-02-SUMMARY.md

@packages/dashboard/src/App.tsx
@packages/dashboard/src/panels/DevicePanel.tsx
@packages/dashboard/src/stores/panel-registry.ts
@packages/dashboard/src/hooks/use-ws.ts
@packages/dashboard/src/components/ModuleShell.tsx
@packages/dashboard/package.json
@packages/dashboard/src/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create zustand stores and Leaflet utilities</name>
  <files>
    packages/dashboard/package.json
    packages/dashboard/src/panels/location/stores/location-store.ts
    packages/dashboard/src/panels/location/stores/playback-store.ts
    packages/dashboard/src/panels/location/stores/route-store.ts
    packages/dashboard/src/panels/location/stores/favorites-store.ts
    packages/dashboard/src/panels/location/markers.ts
    packages/dashboard/src/panels/location/location-panel.css
  </files>
  <action>
Install dashboard dependencies:
```
npm install -w @simvyn/dashboard leaflet react-leaflet @tmcw/togeojson sonner
npm install -w @simvyn/dashboard -D @types/leaflet
```

Create zustand stores:

**location-store.ts:**
```typescript
import { create } from "zustand";

interface LocationState {
  currentLat: number | null;
  currentLon: number | null;
  mode: "point" | "route";
  selectedDeviceId: string | null;
  setCurrentLocation: (lat: number, lon: number) => void;
  setMode: (mode: "point" | "route") => void;
  setSelectedDevice: (id: string | null) => void;
  clearLocation: () => void;
}
```
Default: mode "point", no current location, no selected device.

**playback-store.ts:**
```typescript
interface PlaybackState {
  state: "idle" | "playing" | "paused";
  progress: number;
  currentLat: number | null;
  currentLon: number | null;
  speedMs: number;
  deviceId: string | null;
  setPlaybackState: (state: "idle" | "playing" | "paused") => void;
  setPosition: (lat: number, lon: number, progress: number) => void;
  setSpeed: (speed: number) => void;
  setDeviceId: (id: string | null) => void;
  reset: () => void;
}
```
Default: state "idle", progress 0, speedMs 10.

**route-store.ts:**
```typescript
interface RouteState {
  waypoints: [number, number][];   // [lat, lon] pairs
  addWaypoint: (lat: number, lon: number) => void;
  removeWaypoint: (index: number) => void;
  clearWaypoints: () => void;
  setWaypoints: (waypoints: [number, number][]) => void;
}
```

**favorites-store.ts:**
```typescript
interface FavoritesState {
  locations: SavedLocation[];
  routes: SavedRoute[];
  loading: boolean;
  fetchLocations: () => Promise<void>;
  fetchRoutes: () => Promise<void>;
  addLocation: (name: string, lat: number, lon: number) => Promise<void>;
  removeLocation: (id: string) => Promise<void>;
  addRoute: (name: string, waypoints: [number, number][]) => Promise<void>;
  removeRoute: (id: string) => Promise<void>;
}
```
Define `SavedLocation` and `SavedRoute` types matching the server's storage types (id, name, lat/lon or waypoints, createdAt).
`fetchLocations` and `fetchRoutes` call `GET /api/modules/location/favorites/locations` and `/routes`.
`addLocation` calls `POST /api/modules/location/favorites/locations` and appends to state.
`removeLocation` calls `DELETE /api/modules/location/favorites/locations/:id` and removes from state.
Same pattern for routes.

**markers.ts:**
Create custom Leaflet DivIcon factories:
- `createLocationIcon()`: a pin-shaped div icon with a blue color. Use CSS classes, not inline SVG. Size: 24x36px.
- `createPlaybackIcon()`: a pulsing dot icon for the current playback position. Blue with animation. Size: 16x16px.
- `createWaypointIcon(index: number)`: numbered circle icon for route waypoints. Size: 24x24px.
- `createSearchResultIcon()`: red pin for search result selection. Size: 24x36px.

Each returns a `L.DivIcon` instance. Use `className` to reference CSS classes defined in location-panel.css.

**location-panel.css:**
Import Leaflet CSS at the top: `@import "leaflet/dist/leaflet.css";`

Styles needed:
- `.location-panel` — container that fills the panel's `absolute inset-0` div. Use `display: flex; flex-direction: column; height: 100%;`
- `.location-map` — map container, `flex: 1; min-height: 0;` (critical for flexbox + Leaflet sizing)
- `.location-toolbar` — top bar with search/mode/controls, glass-panel style
- Custom marker icon styles: `.marker-location`, `.marker-playback` (pulsing animation), `.marker-waypoint`, `.marker-search`
- Leaflet control overrides from sim-location's map.css: zoom control styling to match glass-panel aesthetic, transparent popup backgrounds
- Playback controls bar: bottom-positioned overlay, glass-panel style
- Search results dropdown: absolute positioned below search bar
- Favorites sidebar: collapsible panel on the right
- Use oklch color tokens from the existing `@theme` (defined in main.css) where possible
- Keep map tile attribution visible but subtle
  </action>
  <verify>
Run `npm install` to confirm dependencies install. Verify all 4 store files export their hooks (useLocationStore, usePlaybackStore, useRouteStore, useFavoritesStore). Verify markers.ts exports all 4 icon factories. Verify location-panel.css imports leaflet CSS.
  </verify>
  <done>
All zustand stores created with correct state shapes. Leaflet and react-leaflet installed. Custom marker icons defined. CSS file with Leaflet overrides, glass-panel styles, and animation for playback marker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Map components and LocationPanel with WS integration</name>
  <files>
    packages/dashboard/src/panels/LocationPanel.tsx
    packages/dashboard/src/panels/location/MapView.tsx
    packages/dashboard/src/panels/location/SearchBar.tsx
    packages/dashboard/src/panels/location/PlaybackControls.tsx
    packages/dashboard/src/panels/location/FavoritesPanel.tsx
    packages/dashboard/src/panels/location/RouteLayer.tsx
    packages/dashboard/src/panels/location/LocationMarker.tsx
    packages/dashboard/src/panels/location/PlaybackMarker.tsx
    packages/dashboard/src/panels/location/ModeSelector.tsx
    packages/dashboard/src/panels/location/FileImportButton.tsx
    packages/dashboard/src/App.tsx
  </files>
  <action>
Create all map sub-components and the main LocationPanel. Reference sim-location's React components for functionality but reshape for simvyn's Tailwind + glass-panel design system.

**MapView.tsx:**
- Render `<MapContainer>` from react-leaflet with `<TileLayer>` (OpenStreetMap tiles).
- Center: default to San Francisco [37.7749, -122.4194] or last-used location from store.
- Zoom: default 13.
- On map click:
  - In "point" mode: call `POST /api/modules/location/set` with the clicked lat/lon and the selected device. Update locationStore.
  - In "route" mode: add waypoint to routeStore.
- Render child components: LocationMarker, RouteLayer, PlaybackMarker (conditionally).
- The map MUST fill the `.location-map` container. Use `style={{ height: "100%", width: "100%" }}` on MapContainer.

**SearchBar.tsx:**
- Text input with debounced search (300ms). On type, call `GET /api/modules/location/search?q=${query}`.
- Show dropdown with results (display_name, lat/lon).
- On result click: fly map to that location, place a marker, optionally set on device.
- Glass-panel styled input field matching the dashboard design system.

**PlaybackControls.tsx:**
- Horizontal bar at the bottom of the map (overlay).
- Shows: play/pause button, stop button, speed selector (0.5x, 1x, 2x, 5x, 10x), progress bar, current position text.
- Play button: sends WS `{ channel: "location", type: "start-playback", payload: { deviceId, waypoints, speedMs } }`.
- Pause button: sends WS `pause-playback`.
- Stop button: sends WS `stop-playback`.
- Speed change: sends WS `set-speed`.
- Uses playbackStore for state. Visible only when routeStore has waypoints or playback is active.
- Use `useWs().send()` for sending WS messages.

**FavoritesPanel.tsx:**
- Collapsible right sidebar within the location panel.
- Two sections: Saved Locations and Saved Routes.
- Each item shows name, coordinates (or waypoint count), and delete button.
- Click a saved location → fly map there and set on device.
- Click a saved route → load waypoints into routeStore.
- "Save current" button for both: prompts for name (simple inline input), then saves.
- Uses favoritesStore. Fetches on mount.

**RouteLayer.tsx:**
- Renders a Polyline on the map from routeStore waypoints.
- Renders DraggableMarker at each waypoint (using waypointIcon). Drag to reorder.
- Shows total distance and estimated time based on speed.

**LocationMarker.tsx:**
- Renders a Marker at locationStore.currentLat/currentLon using the locationIcon.
- Shows a Popup with coordinates and "Set on device" button (if not auto-set on click).

**PlaybackMarker.tsx:**
- Renders a Marker at playbackStore.currentLat/currentLon using the playbackIcon.
- Only visible when playback state !== "idle".
- Animates smoothly between position updates (CSS transition on transform).

**ModeSelector.tsx:**
- Toggle between "Point" and "Route" modes.
- Two buttons styled as a segmented control with glass-panel aesthetic.
- Updates locationStore.mode.

**FileImportButton.tsx:**
- Hidden file input triggered by a button. Accepts `.gpx` and `.kml` files.
- On file select: read with FileReader, parse with `@tmcw/togeojson` using browser's native DOMParser (client-side parsing).
- Extract [lat, lon] coordinates from LineString features (flip from GeoJSON [lon, lat]).
- Set waypoints in routeStore. Fly map to fit the route bounds.

**LocationPanel.tsx:**
- Main panel component. Layout:
  ```
  <div className="location-panel">
    <div className="location-toolbar">
      <ModeSelector />
      <SearchBar />
      <FileImportButton />
      <FavoritesToggle button />
    </div>
    <div className="location-map">
      <MapView />
      <PlaybackControls /> (absolutely positioned overlay)
    </div>
    <FavoritesPanel /> (conditionally rendered sidebar)
  </div>
  ```
- Import location-panel.css.
- WS integration:
  - On mount, subscribe to "location" channel: `send({ channel: "system", type: "subscribe", payload: { channel: "location" } })`.
  - On unmount, unsubscribe.
  - Use `useWsListener("location", "playback-position", handler)` to update playbackStore position.
  - Use `useWsListener("location", "playback-complete", handler)` to reset playback state.
  - Use `useWsListener("location", "location-set", handler)` to confirm location was set.
  - Use `useWsListener("location", "playback-paused", handler)` and `"playback-resumed"` and `"playback-stopped"` to sync playbackStore.state.
- Register panel: `registerPanel("location", LocationPanel)` at module scope (bottom of file).
- Export default LocationPanel.

**App.tsx:**
Add the side-effect import: `import "./panels/LocationPanel";` alongside the existing `import "./panels/DevicePanel";`.

**Device selection:**
The location panel needs to know which device to set location on. Use the existing deviceStore to get the device list. Add a device picker dropdown or use the TopBar's DeviceSelector. For now, add a simple device dropdown within the location toolbar that reads from useDeviceStore — the first booted device is auto-selected. Store selection in locationStore.selectedDeviceId.
  </action>
  <verify>
Run the Vite dev server: `npm run dev -w @simvyn/dashboard` — the dashboard should build and render. Navigate to the location module in the sidebar — the map should appear. Click on the map — a marker should appear (even if no devices are connected, the UI should not crash). Check browser console for errors.
  </verify>
  <done>
LocationPanel renders in the dashboard with a full Leaflet map. Search bar queries the geocoding proxy. Mode toggle switches between point/route. File import parses GPX/KML client-side. PlaybackControls send WS messages for route simulation. FavoritesPanel shows saved locations/routes. All WS events are handled to sync playback state.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete location module end-to-end</name>
  <files></files>
  <action>
Human verification of the complete location module. No automated action — this is a visual and functional check.

What was built: Complete location module — server routes, WS handler, playback engine, CLI subcommands, and interactive dashboard map panel with search, favorites, and route playback controls.

How to verify:
1. Start the server: `npx tsx packages/cli/src/index.ts`
2. Dashboard opens in browser. Click "location" in sidebar — map should render with OpenStreetMap tiles.
3. Click on map in Point mode — marker appears at clicked location.
4. Type a location name in search bar — results dropdown should appear (requires internet for Nominatim).
5. If an iOS simulator is booted, click map → check simulator's location in Maps.app updates.
6. Switch to Route mode, click multiple points to create a route. See polyline drawn.
7. Import a GPX file via the file import button — route should appear on map.
8. Test CLI: `npx tsx packages/cli/src/index.ts location set <device-prefix> 37.7749 -122.4194`
9. Test CLI: `npx tsx packages/cli/src/index.ts location --help` — shows set, route, clear subcommands.
10. Save a location as favorite — refresh page — favorite should persist.
  </action>
  <verify>All 10 verification steps above produce expected results.</verify>
  <done>Location module works end-to-end: map renders, GPS coordinates can be set on devices, routes can be simulated, search works, favorites persist, CLI commands function headlessly.</done>
</task>

</tasks>

<verification>
1. `npm run dev -w @simvyn/dashboard` compiles without errors
2. LocationPanel appears in sidebar and renders Leaflet map
3. Map click in Point mode sets location (or shows marker if no device)
4. Search bar returns Nominatim results
5. Route mode creates waypoints and draws polyline
6. File import parses GPX/KML and loads route
7. Favorites save and load across page refresh
8. PlaybackControls visible when route has waypoints
9. WS subscription to "location" channel is active when panel is mounted
10. No console errors in dashboard
</verification>

<success_criteria>
The location module's dashboard panel is fully functional: map renders, search works, point and route modes work, file import handles GPX/KML, favorites persist, and playback controls send WS messages. The panel is registered via registerPanel() and auto-discovered by ModuleShell.
</success_criteria>

<output>
After completion, create `.planning/phases/02-location-module/02-04-SUMMARY.md`
</output>
