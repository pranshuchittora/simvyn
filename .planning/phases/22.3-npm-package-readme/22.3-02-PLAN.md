---
phase: 22.3-npm-package-readme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - NPM-02

must_haves:
  truths:
    - "README has the simvyn logo displayed large and centered at the top"
    - "README has the package name and a concise tagline"
    - "README has a feature list covering all major capabilities"
    - "README has installation instructions (npm install -g and npx)"
    - "README has usage examples showing CLI commands and dashboard"
    - "README references screenshots or dashboard preview"
  artifacts:
    - path: "README.md"
      provides: "Professional project README for npm and GitHub"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "assets/simvyn-icon-1024.png"
      via: "markdown image reference"
      pattern: "simvyn-icon"
---

<objective>
Create a professional README.md for the simvyn project — the first thing users see on GitHub and npm.

Purpose: A compelling README is critical for open source adoption. It must communicate what simvyn does, show the dashboard, explain how to install/use it, and list features — all in a scannable format.

Output: A polished README.md at the project root.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@packages/cli/package.json
@assets/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with logo, tagline, features, installation, and usage</name>
  <files>README.md</files>
  <action>
    Create `README.md` at the project root with the following structure:

    **Header section:**
    - Centered simvyn logo using `assets/simvyn-icon-1024.png` — use HTML `<p align="center"><img>` for centering, set width to ~180px
    - Package name "simvyn" as centered `<h1>` below the logo
    - Tagline: "Universal mobile devtool — control iOS Simulators and Android Emulators from a single dashboard and CLI" — centered `<p>` below name
    - npm version badge, license badge, Node.js version badge — centered row of shields.io badges
      - `https://img.shields.io/npm/v/simvyn`
      - `https://img.shields.io/npm/l/simvyn`
      - `https://img.shields.io/node/v/simvyn`

    **Placeholder for screenshot:**
    - A centered comment or placeholder `<!-- Add dashboard screenshot here -->` with text: "📸 *Dashboard screenshot coming soon*" — this will be replaced with an actual screenshot later

    **Quick Start section:**
    ```
    npx simvyn
    ```
    Brief explanation: starts the server, opens the web dashboard, and discovers all connected simulators/emulators.

    **Installation section:**
    - Global install: `npm install -g simvyn`
    - One-time use: `npx simvyn`
    - Requirements: Node.js >= 22.12.0, macOS (full iOS + Android) or Linux (Android only)

    **Features section** — use a concise bullet list with bold feature names:
    - **Device Management** — Discover, boot, shutdown, erase iOS Simulators and Android Emulators
    - **Location Simulation** — Set GPS coordinates, play GPX/KML routes with speed control, save favorite locations
    - **App Management** — Install, launch, terminate, uninstall apps via drag-and-drop or CLI
    - **Log Viewer** — Real-time log streaming with level filtering, regex search, and export
    - **Screenshots & Recording** — Capture screenshots and record screen on any device
    - **Deep Links** — Open URLs and custom schemes, save favorites per app
    - **Push Notifications** — Compose and send push payloads to iOS simulators with template library
    - **File Browser** — Browse app sandboxes, upload/download files, edit text files inline
    - **Database Inspector** — Browse SQLite tables, run SQL queries, view SharedPreferences and NSUserDefaults
    - **Device Settings** — Toggle dark mode, manage permissions, override status bar, accessibility presets
    - **Crash Logs** — View iOS diagnostic reports and Android logcat crashes
    - **Developer Utilities** — Port forwarding, display overrides, battery simulation, input injection, bug reports
    - **Web Dashboard** — Apple Liquid Glass design with command palette (Cmd+K), keyboard navigation
    - **Full CLI** — Every feature works headlessly via `simvyn <command>`

    **CLI Examples section** — show 6-8 common commands:
    ```bash
    simvyn                                    # Start dashboard
    simvyn device list                        # List all devices
    simvyn location set <device> 37.78 -122.41  # Set GPS location
    simvyn app install <device> ./app.apk     # Install an app
    simvyn screenshot <device>                # Take a screenshot
    simvyn logs <device> --level error        # Stream error logs
    ```

    **How It Works section** — 2-3 sentences explaining: simvyn wraps `xcrun simctl` (iOS) and `adb` (Android) behind a unified interface. No SDK required — works with any app, no code changes needed. The web dashboard communicates with a local Fastify server over WebSocket.

    **Supported Platforms section** — simple table:
    | Platform | iOS Simulators | Android Emulators | Android Devices |
    |----------|---------------|-------------------|-----------------|
    | macOS    | Yes           | Yes               | Yes             |
    | Linux    | No            | Yes               | Yes             |

    **License section:**
    MIT

    Do NOT use any emojis in the README. Keep it professional and scannable.
  </action>
  <verify>
    - File exists at `./README.md`
    - Contains the logo image reference pointing to `assets/simvyn-icon-1024.png`
    - Contains shields.io badges for npm version, license, and node version
    - Contains Quick Start, Installation, Features, CLI Examples, How It Works, Supported Platforms, License sections
    - No emojis used (except the camera one in the screenshot placeholder which is acceptable since it's temporary)
  </verify>
  <done>
    README.md exists at project root with: centered logo (180px), package name, tagline, npm/license/node badges, dashboard screenshot placeholder, quick start with `npx simvyn`, installation instructions, 14-item feature list, 6+ CLI examples, how-it-works explanation, platform support table, and MIT license. Professional tone, no emojis, scannable format.
  </done>
</task>

</tasks>

<verification>
- `cat README.md | head -5` shows the centered logo HTML
- `grep -c '##' README.md` shows at least 6 section headings
- `grep 'simvyn-icon-1024' README.md` finds the logo reference
- `grep 'shields.io' README.md` finds badge URLs
- `grep 'npx simvyn' README.md` finds the quick start command
</verification>

<success_criteria>
1. README has centered logo, package name, and tagline at the top
2. README has npm version, license, and Node.js version badges
3. README covers all major features in a scannable bullet list
4. README has clear installation instructions (npm install -g and npx)
5. README has practical CLI usage examples
6. README references how the tool works (simctl + adb, no SDK)
</success_criteria>

<output>
After completion, create `.planning/phases/22.3-npm-package-readme/22.3-02-SUMMARY.md`
</output>
