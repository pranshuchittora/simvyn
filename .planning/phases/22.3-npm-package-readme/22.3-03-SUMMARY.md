---
phase: 22.3-npm-package-readme
plan: 03
subsystem: infra
tags: [github-actions, npm-publish, ci, provenance]

requires:
  - phase: 22.3-npm-package-readme
    provides: "package.json with name, files, bin fields configured for npm"
provides:
  - "Tag-triggered GitHub Actions workflow for automated npm publishing"
  - "npm provenance attestation for supply chain security"
  - "Auto-generated GitHub Releases on version tag push"
affects: [releases, npm-publishing]

tech-stack:
  added: [github-actions, softprops/action-gh-release]
  patterns: [tag-triggered-release, npm-provenance, monorepo-publish]

key-files:
  created: [".github/workflows/release.yml"]
  modified: []

key-decisions:
  - "npm ci instead of bun install — project uses npm workspaces"
  - "Build dashboard before publish — CLI serves built dashboard assets"
  - "npm publish --provenance --ignore-scripts — supply chain security with OIDC attestation"
  - "Separate github-release job after publish — release only created if publish succeeds"

patterns-established:
  - "Tag-triggered release: push v* tag → build → publish → GitHub Release"

requirements-completed: [NPM-03]

duration: 1min
completed: 2026-02-27
---

# Phase 22.3 Plan 03: GitHub Actions Release Workflow Summary

**Tag-triggered CI workflow publishing simvyn to npm with provenance attestation and auto-generated GitHub Releases**

## Performance

- **Duration:** 1 min
- **Started:** 2026-02-27T15:14:57Z
- **Completed:** 2026-02-27T15:15:38Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Created `.github/workflows/release.yml` with tag-triggered npm publish
- Workflow publishes from `packages/cli/` with `--provenance` for supply chain security
- Auto-generates GitHub Releases via `softprops/action-gh-release@v2`
- Includes dashboard build step and tarball verification before publish

## Task Commits

Each task was committed atomically:

1. **Task 1: Create GitHub Actions release workflow** - `d10e82a` (feat)

## Files Created/Modified
- `.github/workflows/release.yml` - Tag-triggered release workflow: npm ci → build → pack verify → publish with provenance → GitHub Release

## Decisions Made
- Used `npm ci` (not `bun install`) since project uses npm workspaces
- Added `npm run build` step before publish — CLI package serves built dashboard assets
- Used `--provenance --ignore-scripts` flags for secure publishing
- Separate `github-release` job with `needs: publish` — release only created if npm publish succeeds
- `npm` GitHub environment for secret management (NPM_TOKEN)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

**External service configuration required before first release:**
- Create a GitHub environment named "npm" in repository settings
- Add `NPM_TOKEN` secret to the "npm" environment (generate from npmjs.com → Access Tokens)
- Ensure the npm package name "simvyn" is available or owned

## Next Phase Readiness
- Phase 22.3 complete (3/3 plans). All NPM package and README work done.
- Ready for phase transition.

## Self-Check: PASSED

- `.github/workflows/release.yml` — exists
- Commit `d10e82a` — found in git log
- `22.3-03-SUMMARY.md` — exists

---
*Phase: 22.3-npm-package-readme*
*Completed: 2026-02-27*
