---
phase: 30.4-device-font-size-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/adapters/android.ts
  - packages/core/src/__tests__/android-adapter.test.ts
autonomous: true
requirements: [FONTSZ-01]

must_haves:
  truths:
    - "Content Size dropdown appears in Accessibility section for Android devices"
    - "Selecting a content size on Android changes the device font scale immediately"
    - "CLI `simvyn a11y content-size` works for Android devices"
    - "Collections `set-content-size` action works for Android devices"
    - "AVD-prefix (unbooted) devices throw a descriptive error"
  artifacts:
    - path: "packages/core/src/adapters/android.ts"
      provides: "setContentSize implementation with font_scale mapping"
      contains: "font_scale"
    - path: "packages/core/src/__tests__/android-adapter.test.ts"
      provides: "setContentSize test coverage"
      contains: "setContentSize"
  key_links:
    - from: "packages/core/src/adapters/android.ts"
      to: "adb shell settings put system font_scale"
      via: "verboseExec in setContentSize"
      pattern: "settings.*put.*system.*font_scale"
    - from: "packages/modules/device-settings/routes.ts"
      to: "adapter.setContentSize"
      via: "capabilities endpoint !!adapter?.setContentSize"
      pattern: "setContentSize"
---

<objective>
Implement Android font scale support via the existing `setContentSize` adapter method.

Purpose: The content size feature (iOS Dynamic Type) already works end-to-end for iOS — dashboard UI, CLI, routes, capabilities, and collections all check `adapter.setContentSize`. Android's adapter explicitly sets it to `undefined`. Implementing this single method auto-enables font size control everywhere for Android.

Output: Android devices show the Content Size dropdown in the Accessibility section of Device Settings.
</objective>

<execution_context>
@/Users/pranshu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/core/src/adapters/android.ts
@packages/core/src/__tests__/android-adapter.test.ts
@packages/modules/device-settings/routes.ts
@packages/dashboard/src/panels/settings/AccessibilitySection.tsx
@packages/modules/collections/action-registry.ts

<interfaces>
<!-- The adapter method signature (already defined, currently `undefined` for Android): -->

From packages/types/src/device.ts:
```typescript
setContentSize?(deviceId: string, size: string): Promise<void>;
```

<!-- Capabilities endpoint auto-derives from adapter method presence: -->

From packages/modules/device-settings/routes.ts (line 268):
```typescript
contentSize: !!adapter?.setContentSize && !isIosPhysical,
```

<!-- Collections action registry auto-checks: -->

From packages/modules/collections/action-registry.ts (line 245):
```typescript
isSupported: (adapter) => !!adapter.setContentSize,
```

<!-- iOS size names used by AccessibilitySection dropdown: -->
```typescript
const CONTENT_SIZES = [
  "extra-small", "small", "medium", "large",
  "extra-large", "extra-extra-large", "extra-extra-extra-large",
  "accessibility-medium", "accessibility-large",
  "accessibility-extra-large", "accessibility-extra-extra-large",
  "accessibility-extra-extra-extra-large",
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Android setContentSize with font_scale mapping</name>
  <files>packages/core/src/adapters/android.ts, packages/core/src/__tests__/android-adapter.test.ts</files>
  <action>
In `packages/core/src/adapters/android.ts`, replace `setContentSize: undefined,` with an async method implementation:

1. Add a `CONTENT_SIZE_TO_FONT_SCALE` map at module level (before `createAndroidAdapter`) mapping iOS Dynamic Type size names to Android font_scale float values:
   ```
   extra-small → 0.85
   small → 0.90
   medium → 1.0
   large → 1.1
   extra-large → 1.2
   extra-extra-large → 1.3
   extra-extra-extra-large → 1.4
   accessibility-medium → 1.5
   accessibility-large → 1.7
   accessibility-extra-large → 2.0
   accessibility-extra-extra-large → 2.5
   accessibility-extra-extra-extra-large → 3.0
   ```

2. Implement `setContentSize`:
   - Guard: `if (deviceId.startsWith("avd:")) throw new Error("Device must be booted to change font scale");`
   - Look up `size` in the map. If not found, throw `new Error("Unknown content size: {size}")`.
   - Execute: `await verboseExec("adb", ["-s", deviceId, "shell", "settings", "put", "system", "font_scale", scale.toString()]);`

3. In `packages/core/src/__tests__/android-adapter.test.ts`, add a `describe("setContentSize", ...)` block following the existing `setOrientation` test pattern:
   - Test "medium" maps to font_scale 1.0 → asserts `verboseExec` called with `["adb", "-s", "emulator-5554", "shell", "settings", "put", "system", "font_scale", "1"]`
   - Test "extra-large" maps to font_scale 1.2 → asserts correct adb args
   - Test "accessibility-large" maps to font_scale 1.7 → asserts correct adb args
   - Test unknown size throws "Unknown content size" error
   - Test avd: prefix throws "Device must be booted" error
   - Add `setContentSize` to the avd-guard bulk test list (alongside setOrientation, setAppearance, etc.)

No changes needed to routes.ts, AccessibilitySection.tsx, CLI, or action-registry — all auto-enable via `!!adapter.setContentSize` capability check.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -40</automated>
  </verify>
  <done>
    - Android adapter has `setContentSize` method (not `undefined`)
    - Map covers all 12 iOS content size names
    - `adb shell settings put system font_scale` called with correct mapped value
    - AVD-prefix guard prevents calls on unbooted devices
    - All existing + new tests pass
  </done>
</task>

</tasks>

<verification>
- `npm test` passes (all existing tests + new setContentSize tests)
- `npm run build` succeeds (no type errors)
- Capabilities endpoint returns `contentSize: true` for Android emulators (auto-derived from adapter method)
- AccessibilitySection renders Content Size dropdown for Android devices (auto-derived from capabilities)
</verification>

<success_criteria>
Android devices show the Content Size dropdown in Device Settings → Accessibility section. Selecting a size changes the device's font scale via `adb shell settings put system font_scale`. The same feature works in CLI (`simvyn a11y content-size`) and Collections (`set-content-size` action). All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/30.4-device-font-size-control/30.4-01-SUMMARY.md`
</output>
