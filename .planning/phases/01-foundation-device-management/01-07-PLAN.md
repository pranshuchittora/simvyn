---
phase: 01-foundation-device-management
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: ["packages/dashboard/src/hooks/use-ws.ts"]
autonomous: true
requirements: [DEV-06]
gap_closure: true

must_haves:
  truths:
    - "WebSocket devices channel broadcasts device list updates in real-time to the dashboard"
    - "Dashboard client receives device-list messages from server when DeviceManager emits devices-changed"
  artifacts:
    - path: "packages/dashboard/src/hooks/use-ws.ts"
      provides: "WebSocket connection with correct subscription envelope"
      contains: 'channel: "system"'
  key_links:
    - from: "packages/dashboard/src/hooks/use-ws.ts"
      to: "packages/server/src/ws-broker.ts"
      via: "WS subscribe message on system channel with payload.channel"
      pattern: 'channel.*system.*subscribe.*payload.*channel.*devices'
---

<objective>
Fix WebSocket subscription wiring so the dashboard receives real-time device updates.

Purpose: The dashboard sends the subscribe message to the wrong channel (`devices` instead of `system`), so the ws-broker never adds the client to the devices subscription set. This single-line fix closes the only verification gap in Phase 1.
Output: Corrected `use-ws.ts` with proper subscribe envelope format.
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-device-management/01-VERIFICATION.md

@packages/dashboard/src/hooks/use-ws.ts
@packages/server/src/ws-broker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WS subscribe envelope in use-ws.ts</name>
  <files>packages/dashboard/src/hooks/use-ws.ts</files>
  <action>
In `packages/dashboard/src/hooks/use-ws.ts`, line 42, change the subscribe message sent on WebSocket open from:

```ts
socket.send(JSON.stringify({ channel: "devices", type: "subscribe", payload: null }));
```

to:

```ts
socket.send(JSON.stringify({ channel: "system", type: "subscribe", payload: { channel: "devices" } }));
```

This matches the ws-broker protocol in `packages/server/src/ws-broker.ts` lines 74-85: the broker only processes subscribe/unsubscribe messages on the `system` channel, extracting the target channel name from `payload.channel`. Without this fix, the client never gets added to the `devices` subscription set, so `wsBroker.broadcast("devices", ...)` calls (triggered by DeviceManager "devices-changed" events) never reach the dashboard.

Do NOT change anything else in this file. The rest of the WebSocket logic (reconnect, listener dispatch, send helper) is correct.
  </action>
  <verify>
1. `grep -n 'channel.*system.*subscribe' packages/dashboard/src/hooks/use-ws.ts` shows the corrected line with channel "system" and payload containing "devices"
2. `grep -c '"devices".*"subscribe"' packages/dashboard/src/hooks/use-ws.ts` returns 0 (old pattern gone)
3. `npx tsc --noEmit -p packages/dashboard/tsconfig.json` compiles without errors
  </verify>
  <done>Line 42 of use-ws.ts sends `{channel: "system", type: "subscribe", payload: {channel: "devices"}}` — the ws-broker will now add the client to the devices subscription set and real-time device list updates will be delivered to the dashboard.</done>
</task>

</tasks>

<verification>
1. The subscribe envelope in use-ws.ts uses channel "system" with payload `{channel: "devices"}`
2. TypeScript compiles cleanly for the dashboard package
3. No other files were modified — this is a surgical single-line fix
</verification>

<success_criteria>
- DEV-06 gap closed: dashboard WebSocket subscription matches ws-broker protocol
- Real-time device updates will flow from server to dashboard when DeviceManager emits "devices-changed"
- Phase 1 verification score moves from 16/17 to 17/17
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-device-management/01-07-SUMMARY.md`
</output>
