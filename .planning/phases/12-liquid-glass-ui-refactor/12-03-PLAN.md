---
phase: 12-liquid-glass-ui-refactor
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - packages/dashboard/src/panels/ScreenshotPanel.tsx
  - packages/dashboard/src/panels/CrashLogsPanel.tsx
  - packages/dashboard/src/panels/MediaPanel.tsx
  - packages/dashboard/src/panels/ClipboardPanel.tsx
  - packages/dashboard/src/panels/DeepLinksPanel.tsx
  - packages/dashboard/src/panels/PushPanel.tsx
autonomous: true
requirements: [GLASS-03]

must_haves:
  truths:
    - "All 6 panels use shared glass utility classes from main.css — no inline Tailwind duplicating glass properties"
    - "Device selects use .glass-select, primary buttons use .glass-button-primary, destructive buttons use .glass-button-destructive"
    - "Empty states use .glass-empty-state"
    - "Textareas (Push JSON editor) use .glass-textarea"
    - "Inputs (Deep Links URL, Push bundle ID) use .glass-input"
  artifacts:
    - path: "packages/dashboard/src/panels/ScreenshotPanel.tsx"
      provides: "Screenshot panel with glass utilities"
    - path: "packages/dashboard/src/panels/PushPanel.tsx"
      provides: "Push panel with glass-textarea, glass-input, glass-button-primary"
    - path: "packages/dashboard/src/panels/DeepLinksPanel.tsx"
      provides: "Deep links panel with glass-input, glass-button-primary"
  key_links:
    - from: "packages/dashboard/src/panels/ScreenshotPanel.tsx"
      to: "packages/dashboard/src/main.css"
      via: "glass utility classNames"
      pattern: "glass-(select|button-primary|empty-state)"
---

<objective>
Convert 6 small and medium-small panels to use shared Liquid Glass utility classes.

Purpose: These panels (Screenshot, CrashLogs, Media, Clipboard, DeepLinks, Push) are the simplest conversions — they use glass-panel for containers but inline Tailwind for buttons, selects, and inputs. Mechanical className replacement using the shared utilities from Plan 01.

Output: 6 panel files converted to use glass-select, glass-button-primary, glass-button-destructive, glass-input, glass-textarea, glass-empty-state consistently.
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-liquid-glass-ui-refactor/12-RESEARCH.md
@.planning/phases/12-liquid-glass-ui-refactor/12-01-SUMMARY.md
@packages/dashboard/src/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Screenshot, CrashLogs, Media, Clipboard panels</name>
  <files>packages/dashboard/src/panels/ScreenshotPanel.tsx, packages/dashboard/src/panels/CrashLogsPanel.tsx, packages/dashboard/src/panels/MediaPanel.tsx, packages/dashboard/src/panels/ClipboardPanel.tsx</files>
  <action>
For each of the 4 panels, apply these mechanical conversions:

**Pattern A — Device select (appears in every panel):**
Find: `<select className="rounded-[var(--radius-button)] bg-bg-surface/60 border border-border px-2 py-1.5 text-xs text-text-secondary ...">`
Replace with: `<select className="glass-select ...">` (keep non-glass classes like max-w-[200px] truncate)

**Pattern B — Primary action buttons (blue accent):**
Find: buttons with `bg-accent-blue/20 border border-accent-blue/30 ... text-accent-blue hover:bg-accent-blue/30` or similar blue-accent styling
Replace with: `className="glass-button-primary flex items-center gap-2"` (keep layout utility classes like flex, items-center, gap-*)

**Pattern C — Standard buttons (neutral):**
Find: buttons with `bg-bg-surface/60 border border-border ... hover:bg-bg-surface/80` or similar neutral styling
Replace with: `className="glass-button flex items-center gap-2"` (keep layout classes)

**Pattern D — Empty states:**
Find: empty state divs with centered text, text-text-secondary, padding
Replace with: `className="glass-empty-state"` or add glass-empty-state alongside existing layout classes

**Pattern E — Textareas (Clipboard panel):**
Find: `<textarea className="... bg-transparent ... font-mono ...">`
Replace with: `<textarea className="glass-textarea ...">`

**Per-panel specifics:**

*ScreenshotPanel.tsx:* 1 device select (A), capture button (B), record start/stop button (B), download button (C), copy button (C). Cards already use glass-panel. ~5 replacements.

*CrashLogsPanel.tsx:* 1 device select (A), refresh button (C), back button (C), app filter input → glass-input, date filter input → glass-input. ~5 replacements.

*MediaPanel.tsx:* 1 device select (A), browse/upload button (B), drop zone → glass-drop-zone. The drag-over state should add `.drag-over` class. ~3 replacements.

*ClipboardPanel.tsx:* 1 device select (A), read/copy/write/paste buttons (mix B and C), 2 textareas → glass-textarea. ~7 replacements.

IMPORTANT: Keep all conditional logic (disabled states, active states, onClick handlers). Only replace the STATIC className portions. For conditional classes like `${isRecording ? 'bg-red-500/20' : 'bg-accent-blue/20'}`, convert to use glass-button-primary or glass-button-destructive with the conditional.
  </action>
  <verify>Run `npm run build --workspace=packages/dashboard`. Grep all 4 files for `bg-bg-surface/60 border border-border` — should find zero hits (all converted to glass utilities). Grep for `glass-select` — should find at least 4 hits (one per panel).</verify>
  <done>All 4 panels use shared glass utility classes. No inline Tailwind duplicates glass properties. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Convert DeepLinks and Push panels</name>
  <files>packages/dashboard/src/panels/DeepLinksPanel.tsx, packages/dashboard/src/panels/PushPanel.tsx</files>
  <action>
Apply the same mechanical conversions (Patterns A-E from Task 1) plus:

**Pattern F — Text inputs:**
Find: `<input className="... bg-bg-surface/60 border border-border ... rounded-[var(--radius-button)] ...">`
Replace with: `<input className="glass-input ...">`

**DeepLinksPanel.tsx specifics:**
- 1 device select (A)
- "Open" primary button (B) → glass-button-primary
- Add/Cancel/Save form buttons → glass-button-primary for Save, glass-button for Cancel
- URL input → glass-input
- Favorite URL, label, bundleId inputs → glass-input
- Favorites list items already use glass-panel (keep)
- Empty state (D)
- ~8-10 replacements

**PushPanel.tsx specifics:**
- 1 device select (A)
- "Send" primary button (B) → glass-button-primary
- "Save" / "Save Current" buttons (C) → glass-button
- History toggle button (C) → glass-button
- Bundle ID input → glass-input
- Save name input → glass-input
- JSON payload textarea → glass-textarea
- Templates/saved payloads cards already use glass-panel
- iOS-only badge → glass-badge (with blue tint: style text color to accent-blue, border-color to accent-blue/30)
- Empty state (D)
- ~10-12 replacements

IMPORTANT: The Push panel's JSON validation with red border on invalid JSON should still work. Keep the conditional `border-red-500` class alongside glass-textarea — CSS class order will let the conditional override.
  </action>
  <verify>Run `npm run build --workspace=packages/dashboard`. Grep DeepLinksPanel.tsx and PushPanel.tsx for `bg-bg-surface/60` — should find zero. Grep for `glass-input` — should find 5+ hits across both files. Grep for `glass-textarea` — should find 1+ hit in PushPanel.</verify>
  <done>DeepLinks and Push panels use shared glass utilities. JSON validation red border still works conditionally. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build --workspace=packages/dashboard` passes
- All 6 panel files use glass-select for device dropdowns
- No inline Tailwind patterns that duplicate glass utility properties
- Conditional styling (disabled, active, recording states) still works
</verification>

<success_criteria>
6 panels visually match the Liquid Glass aesthetic using shared utility classes. Screenshot, CrashLogs, Media, Clipboard, DeepLinks, and Push panels are consistent with each other and with the glass design system.
</success_criteria>

<output>
After completion, create `.planning/phases/12-liquid-glass-ui-refactor/12-03-SUMMARY.md`
</output>
