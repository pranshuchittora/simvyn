---
phase: 12-liquid-glass-ui-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/main.css
  - packages/dashboard/src/panels/location/location-panel.css
autonomous: true
requirements: [GLASS-01]

must_haves:
  truths:
    - "Body background is noticeably lighter than before — dark charcoal/navy, not near-black"
    - "All shared glass utility classes exist in main.css and can be used by any panel"
    - "location-panel.css has no duplicate .glass-panel or .glass-button definitions"
    - "prefers-reduced-motion media query disables transitions and reduces blur"
  artifacts:
    - path: "packages/dashboard/src/main.css"
      provides: "Design tokens, 15+ utility classes, lighter color palette"
      contains: "glass-button-primary"
    - path: "packages/dashboard/src/panels/location/location-panel.css"
      provides: "Location-specific styles only, no duplicates of main.css"
  key_links:
    - from: "packages/dashboard/src/main.css"
      to: "all panel components"
      via: "className references"
      pattern: "glass-(button|select|input|table|tab|badge|drop-zone|textarea|empty-state)"
---

<objective>
Expand the shared design system with a lighter color palette, new glass utility classes, and clean up location-panel.css conflicts.

Purpose: Establish the single source of truth for all Liquid Glass visual properties before panels are converted. The current palette is too dark (near-black) — shift to a dark charcoal/navy that lets glass surfaces breathe. Add utility classes for every recurring UI pattern (buttons, tables, tabs, badges, etc.) so panel conversion is mechanical className swaps.

Output: Updated main.css with lighter tokens + 8 new utility classes + accessibility media queries. Cleaned location-panel.css without duplicate definitions.
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-liquid-glass-ui-refactor/12-RESEARCH.md
@packages/dashboard/src/main.css
@packages/dashboard/src/panels/location/location-panel.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lighten color tokens and add shared utility classes to main.css</name>
  <files>packages/dashboard/src/main.css</files>
  <action>
Update the @theme block to lighten the entire palette. The user said the current UI is "too dark." Current body gradient uses oklch lightness 0.04-0.06 (near-black). Shift to 0.08-0.14 range (dark charcoal/navy). Approximate new values:

**@theme token updates (lighten by ~0.04-0.06):**
- --color-bg-base: oklch(0.05 ...) → oklch(0.10 0.012 270) — still dark but not black
- --color-bg-surface: oklch(0.12 ...) → oklch(0.17 0.014 270) — medium-dark surface
- --color-bg-elevated: oklch(0.16 ...) → oklch(0.21 0.016 270) — slightly lighter
- --color-glass: oklch(0.14 ... / 0.55) → oklch(0.19 0.015 270 / 0.50) — medium translucent
- --color-glass-hover: oklch(0.18 ... / 0.7) → oklch(0.24 0.015 270 / 0.65)
- --color-text-muted: oklch(0.4 ...) → oklch(0.45 0.005 260) — slightly more visible

**Body gradient (lighten to charcoal/navy):**
Replace the 4-stop gradient with similar hues but lighter:
- 0%: oklch(0.04 0.02 280) → oklch(0.09 0.02 280)
- 30%: oklch(0.05 0.015 260) → oklch(0.10 0.016 260)
- 60%: oklch(0.045 0.012 240) → oklch(0.085 0.013 240)
- 100%: oklch(0.06 0.02 270) → oklch(0.11 0.02 270)

**Update .glass-panel background and shadow** to match lighter palette:
- background: rgba(30, 30, 40, 0.55) → rgba(40, 40, 55, 0.50) (slightly lighter, slightly less opaque)
- Keep same blur(20px) saturate(1.4)
- Adjust inset highlight: rgba(255,255,255,0.06) → rgba(255,255,255,0.08) (slightly brighter)

**Update .top-bar and .dock-sidebar backgrounds** to lighter:
- rgba(16, 16, 24, 0.7) → rgba(28, 28, 40, 0.65)

**Update .glass-button, .glass-input, .glass-select** backgrounds slightly lighter to match.

**Add these NEW utility classes after the existing ones:**

1. `.glass-button-primary` — blue action button:
   - background: rgba(0, 122, 255, 0.25), border: 1px solid rgba(0, 122, 255, 0.35)
   - border-radius: var(--radius-button), padding: 6px 12px
   - color: rgba(100, 180, 255, 1), cursor: pointer, font-size: 0.75rem, font-family: inherit
   - transition: background 0.15s ease
   - :hover → background: rgba(0, 122, 255, 0.4)
   - :disabled → opacity: 0.4, cursor: not-allowed

2. `.glass-button-destructive` — red danger button:
   - background: rgba(255, 107, 107, 0.12), color: #ff6b6b
   - border: 1px solid rgba(255, 107, 107, 0.2)
   - Same radius/padding/font as glass-button-primary
   - :hover → background: rgba(255, 107, 107, 0.25)

3. `.glass-tab-bar` — segmented control container:
   - display: flex, align-items: center, gap: 2px, padding: 2px
   - border-radius: 12px, background: rgba(255,255,255,0.03)
   - border: 1px solid rgba(255,255,255,0.04), width: fit-content

4. `.glass-tab` — individual tab in tab bar:
   - padding: 6px 16px, border-radius: 10px, font-size: 0.75rem, font-weight: 500
   - color: var(--color-text-secondary), cursor: pointer
   - transition: all 0.15s ease, border: none, background: transparent, font-family: inherit
   - `.glass-tab.active` → background: var(--color-glass), color: var(--color-text-primary), box-shadow: 0 1px 4px rgba(0,0,0,0.2)
   - `.glass-tab:hover:not(.active)` → color: var(--color-text-primary)

5. `.glass-table` — data table:
   - width: 100%, font-size: 0.875rem
   - `thead tr` → border-bottom: 1px solid rgba(255,255,255,0.08), text-align: left
   - `thead th` → padding: 8px 16px, font-size: 0.75rem, font-weight: 500, color: var(--color-text-muted), text-transform: uppercase, letter-spacing: 0.05em
   - `tbody tr` → border-bottom: 1px solid rgba(255,255,255,0.04), transition: background 0.1s ease
   - `tbody tr:hover` → background: rgba(255,255,255,0.02)
   - `td` → padding: 6px 16px

6. `.glass-badge` — colored pill:
   - display: inline-flex, align-items: center, border-radius: 9999px
   - border: 1px solid, padding: 2px 8px
   - font-size: 0.6875rem, font-weight: 500

7. `.glass-drop-zone` — file upload area:
   - border: 2px dashed rgba(255,255,255,0.08)
   - border-radius: var(--radius-panel), padding: 48px, text-align: center
   - transition: border-color 0.15s ease, background 0.15s ease
   - `.glass-drop-zone.drag-over` → border-color: rgba(0,122,255,0.4), background: rgba(0,122,255,0.04)

8. `.glass-textarea` — code/JSON editor:
   - width: 100%, background: transparent
   - color: var(--color-text-primary)
   - font-family: "SF Mono", "Fira Code", ui-monospace, monospace
   - font-size: 0.875rem, resize: none, outline: none, padding: 12px

9. `.glass-empty-state` — no data placeholder:
   - text-align: center, padding: 48px
   - color: var(--color-text-secondary), font-size: 0.875rem

**Add at the very end — accessibility media queries:**
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    transition-duration: 0.01ms !important;
    animation-duration: 0.01ms !important;
  }
  .glass-panel, .top-bar, .dock-sidebar {
    backdrop-filter: blur(8px) saturate(1.1);
    -webkit-backdrop-filter: blur(8px) saturate(1.1);
  }
}
```

Also update the Sonner toast overrides to match lighter palette (rgba(30,30,40,0.7) → rgba(40,40,55,0.65)).

Also update scrollbar thumb colors to match lighter palette.
  </action>
  <verify>Open packages/dashboard/src/main.css. Confirm: @theme has lighter oklch values, body gradient uses 0.08-0.14 range, all 9 new utility classes exist (glass-button-primary, glass-button-destructive, glass-tab-bar, glass-tab, glass-table, glass-badge, glass-drop-zone, glass-textarea, glass-empty-state), prefers-reduced-motion block exists at end. Run `npm run build --workspace=packages/dashboard` to verify CSS compiles without errors.</verify>
  <done>main.css has lighter color tokens, 15+ total utility classes (7 existing + 8 new), and accessibility media queries. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate definitions from location-panel.css</name>
  <files>packages/dashboard/src/panels/location/location-panel.css</files>
  <action>
Remove the duplicate `.glass-panel` and `.glass-button` class definitions from location-panel.css. These duplicate what main.css provides and can cause specificity conflicts. They were carried over from the standalone sim-location app.

Search for `.glass-panel` definition blocks in location-panel.css (should be around lines 366-394 area based on research). Remove the entire `.glass-panel { ... }` block and any `.glass-button` block that duplicates main.css.

Also search for any hardcoded `border-radius: 16px` values and replace with `var(--radius-panel)` to use the token (14px). Exception: keep hardcoded values inside Leaflet-specific overrides (`.leaflet-*` selectors) since those override third-party library styles.

Do NOT remove location-specific classes like `.location-panel`, `.search-bar`, `.mode-selector`, `.playback-controls`, etc. — those are unique to the location panel. Only remove classes that duplicate main.css utilities.
  </action>
  <verify>Grep location-panel.css for `.glass-panel {` and `.glass-button {` — should find zero standalone definitions (only as part of compound selectors like `.some-location-class .glass-panel` which are fine). Run `npm run build --workspace=packages/dashboard` to verify no broken CSS references.</verify>
  <done>location-panel.css contains only location-specific styles. No duplicate .glass-panel or .glass-button definitions. Hardcoded 16px border-radius replaced with var(--radius-panel) where appropriate. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build --workspace=packages/dashboard` passes
- main.css @theme tokens are measurably lighter (bg-base lightness ≥ 0.09)
- All 9 new utility classes present in main.css
- location-panel.css has no standalone .glass-panel or .glass-button blocks
- prefers-reduced-motion media query present
</verification>

<success_criteria>
main.css is the complete single source of truth for all glass visual properties. Any panel can use glass-button-primary, glass-table, glass-tab-bar, etc. without defining its own versions. Color palette is lighter. location-panel.css has no conflicts.
</success_criteria>

<output>
After completion, create `.planning/phases/12-liquid-glass-ui-refactor/12-01-SUMMARY.md`
</output>
