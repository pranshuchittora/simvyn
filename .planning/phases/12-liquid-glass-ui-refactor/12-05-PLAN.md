---
phase: 12-liquid-glass-ui-refactor
plan: 05
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - packages/dashboard/src/panels/FileSystemPanel.tsx
  - packages/dashboard/src/panels/file-system/FileBrowser.tsx
  - packages/dashboard/src/panels/file-system/FileEditor.tsx
  - packages/dashboard/src/panels/DatabasePanel.tsx
  - packages/dashboard/src/panels/database/DatabaseBrowser.tsx
  - packages/dashboard/src/panels/database/TableViewer.tsx
  - packages/dashboard/src/panels/database/SqlEditor.tsx
  - packages/dashboard/src/panels/database/PrefsViewer.tsx
autonomous: true
requirements: [GLASS-03]

must_haves:
  truths:
    - "File browser table uses .glass-table, file editor textarea uses .glass-textarea"
    - "Database tab bar uses .glass-tab-bar and .glass-tab classes"
    - "All 3 database tables (database browser, table viewer, prefs viewer) use .glass-table"
    - "SQL editor textarea uses .glass-textarea"
    - "No glass-on-glass nesting violations — inner panels use transparent fills, not .glass-panel"
  artifacts:
    - path: "packages/dashboard/src/panels/database/TableViewer.tsx"
      provides: "Table viewer with glass-table and glass-tab-bar"
    - path: "packages/dashboard/src/panels/file-system/FileBrowser.tsx"
      provides: "File browser with glass-table"
    - path: "packages/dashboard/src/panels/database/SqlEditor.tsx"
      provides: "SQL editor with glass-textarea"
  key_links:
    - from: "packages/dashboard/src/panels/DatabasePanel.tsx"
      to: "packages/dashboard/src/main.css"
      via: "glass-tab-bar, glass-tab classNames"
      pattern: "glass-tab"
    - from: "packages/dashboard/src/panels/database/TableViewer.tsx"
      to: "packages/dashboard/src/main.css"
      via: "glass-table className"
      pattern: "glass-table"
---

<objective>
Convert FileSystem and Database panels (including all sub-components) to use shared Liquid Glass utility classes.

Purpose: These panels have the most complex UI patterns — file trees with tables, database tab bars, paginated table viewers, SQL editors, and preference viewers. The Database panel is the most component-rich panel in the dashboard (5 sub-components). Converting these ensures the glass design system covers even the most complex UI patterns.

Output: 8 component files converted with glass-table, glass-tab-bar, glass-tab, glass-textarea, glass-select, glass-button variants.
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-liquid-glass-ui-refactor/12-RESEARCH.md
@.planning/phases/12-liquid-glass-ui-refactor/12-01-SUMMARY.md
@packages/dashboard/src/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert FileSystem panel system</name>
  <files>packages/dashboard/src/panels/FileSystemPanel.tsx, packages/dashboard/src/panels/file-system/FileBrowser.tsx, packages/dashboard/src/panels/file-system/FileEditor.tsx</files>
  <action>
**FileSystemPanel.tsx:**
- Device select → glass-select
- App select → glass-select
- Empty state → glass-empty-state
- Container — verify uses glass-panel, no glass-on-glass nesting

**FileBrowser.tsx:**
- File list table → glass-table on the `<table>` element
- Remove inline Tailwind on thead/tr/th/td that duplicates glass-table (border-b, padding, text-xs uppercase for headers)
- Keep file-specific styling: icon column width, filename truncation, size alignment
- Directory/file name text → var(--color-text-primary), size/date → var(--color-text-secondary)
- Breadcrumb path segments — these are navigation, can use subtle glass-like hover (bg rgba on hover), but NOT full glass-panel
- Download button per row → glass-button (small variant: add text-xs)
- Upload button → glass-button-primary
- Back/parent directory button → glass-button

**FileEditor.tsx:**
- Text editor textarea → glass-textarea
- Save button → glass-button-primary
- Close button → glass-button
- The editor container — if it's inside a glass-panel, ensure no glass-on-glass. The editor should use a transparent or subtle bg (bg-bg-surface/20 at most), not another glass-panel.
- Filename/path header text → var(--color-text-secondary)

IMPORTANT: File browser likely has click-to-navigate behavior on directories. Keep all onClick handlers and hover states. Only replace visual classes.
  </action>
  <verify>Run `npm run build --workspace=packages/dashboard`. Grep FileBrowser.tsx for `glass-table` — should find 1 hit. Grep FileEditor.tsx for `glass-textarea` — should find 1 hit. No glass-on-glass nesting (grep for nested `glass-panel` usage).</verify>
  <done>FileSystem panel uses glass-table for file listing, glass-textarea for editor, glass-button variants for actions. No glass-on-glass nesting. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Database panel system</name>
  <files>packages/dashboard/src/panels/DatabasePanel.tsx, packages/dashboard/src/panels/database/DatabaseBrowser.tsx, packages/dashboard/src/panels/database/TableViewer.tsx, packages/dashboard/src/panels/database/SqlEditor.tsx, packages/dashboard/src/panels/database/PrefsViewer.tsx</files>
  <action>
**DatabasePanel.tsx:**
- Device select → glass-select
- App select → glass-select
- Tab bar (Tables / SQL / Preferences) → replace inline Tailwind with `.glass-tab-bar` on container and `.glass-tab` on each tab button. Active tab gets additional `.active` class. The research notes current tab bar uses "glass-panel segment control (bg-bg-surface/40 with rounded-xl)" — replace with the shared `.glass-tab-bar` / `.glass-tab` pattern.
- Empty state → glass-empty-state
- Container — check for glass-on-glass: the DatabaseBrowser and TableViewer are rendered INSIDE the panel. If the outer panel is glass-panel and inner components also use glass-panel, remove glass-panel from inner components and use transparent/subtle backgrounds instead.

**DatabaseBrowser.tsx:**
- Database list (tables) → glass-table
- Table name links → use var(--color-accent-blue) for clickable names
- Row count badge → glass-badge
- Refresh button → glass-button
- Empty state → glass-empty-state

**TableViewer.tsx:**
- Data table → glass-table
- Sticky table headers — currently use `bg-bg-surface/90 backdrop-blur-sm` (a different glass treatment from research). Replace with a simple `background: var(--color-bg-surface)` for sticky headers (NOT backdrop-filter — that's glass-on-glass since it's inside a glass-panel container). Use opaque bg for sticky so content behind is hidden.
- Pagination controls → glass-button for prev/next, glass-select for page size dropdown
- Sort buttons on column headers → keep inline, small
- Inline cell editing (double-click) — keep existing behavior. The input that appears on edit should use glass-input appearance.
- Column resizing handles — keep existing behavior

**SqlEditor.tsx:**
- SQL textarea → glass-textarea
- Run query button → glass-button-primary
- Results table → glass-table
- Error display — keep red-tinted styling for SQL errors
- Empty state → glass-empty-state

**PrefsViewer.tsx:**
- Preferences table(s) → glass-table
- On Android: grouped by file — section headers should use var(--color-text-muted) uppercase text, not glass-panel
- Type badges (string, int, bool, float, etc.) → glass-badge with type-specific colors
- Edit inputs → glass-input
- Empty state → glass-empty-state

GLASS-ON-GLASS AUDIT: The Database panel nests multiple components. Ensure only the OUTERMOST container (DatabasePanel.tsx) uses glass-panel. Inner components (DatabaseBrowser, TableViewer, SqlEditor, PrefsViewer) should NOT have their own glass-panel wrapper. They should use transparent or very subtle backgrounds (bg-transparent or bg-bg-surface/10 at most).
  </action>
  <verify>Run `npm run build --workspace=packages/dashboard`. Grep DatabasePanel.tsx for `glass-tab-bar` — should find 1 hit. Grep for `glass-tab` — should find 3+ hits (one per tab). Grep all 5 database files for `glass-table` — should find 3-4 hits. Verify no inner components have standalone `glass-panel` class (only the outer DatabasePanel should).</verify>
  <done>Database panel uses glass-tab-bar/glass-tab for navigation, glass-table for all 3 table views, glass-textarea for SQL editor. No glass-on-glass nesting. Type badges use glass-badge. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build --workspace=packages/dashboard` passes
- FileSystem uses glass-table and glass-textarea
- Database uses glass-tab-bar, glass-table (3x), glass-textarea
- No glass-on-glass nesting in either panel
- Sticky table headers use opaque bg (not backdrop-filter)
- Inline editing and pagination still work
</verification>

<success_criteria>
FileSystem and Database panels — the two most complex data-heavy panels — look consistent with the Liquid Glass aesthetic. Tables, tabs, editors, and badges all use shared utilities. Glass-on-glass violations fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/12-liquid-glass-ui-refactor/12-05-SUMMARY.md`
</output>
