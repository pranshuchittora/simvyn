---
phase: 12-liquid-glass-ui-refactor
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - packages/dashboard/src/panels/DevicePanel.tsx
  - packages/dashboard/src/panels/AppPanel.tsx
  - packages/dashboard/src/panels/apps/AppList.tsx
  - packages/dashboard/src/panels/apps/AppActions.tsx
  - packages/dashboard/src/panels/apps/InstallDropZone.tsx
  - packages/dashboard/src/panels/LogPanel.tsx
  - packages/dashboard/src/panels/logs/LogToolbar.tsx
  - packages/dashboard/src/panels/logs/LogList.tsx
autonomous: true
requirements: [GLASS-03]

must_haves:
  truths:
    - "Device panel cards, badges, and action buttons use shared glass utilities"
    - "App panel table uses .glass-table, drop zone uses .glass-drop-zone, buttons use glass-button variants"
    - "Log panel toolbar buttons use glass-button, filters use glass-input and glass-select"
    - "No inline Tailwind duplicating glass properties in any of the 8 files"
  artifacts:
    - path: "packages/dashboard/src/panels/DevicePanel.tsx"
      provides: "Device panel with glass-badge, glass-button, glass-button-destructive"
    - path: "packages/dashboard/src/panels/apps/AppList.tsx"
      provides: "App list table with glass-table"
    - path: "packages/dashboard/src/panels/logs/LogToolbar.tsx"
      provides: "Log toolbar with glass-button, glass-input, glass-select"
  key_links:
    - from: "packages/dashboard/src/panels/apps/AppList.tsx"
      to: "packages/dashboard/src/main.css"
      via: "glass-table className"
      pattern: "glass-table"
    - from: "packages/dashboard/src/panels/apps/InstallDropZone.tsx"
      to: "packages/dashboard/src/main.css"
      via: "glass-drop-zone className"
      pattern: "glass-drop-zone"
---

<objective>
Convert Device, App, and Log panels (including sub-components) to use shared Liquid Glass utility classes.

Purpose: These are the medium-complexity panels that form the core workflow (device selection → app management → log viewing). Each has multiple sub-components with tables, toolbars, drop zones, and filter controls that currently use inline Tailwind. Convert to shared glass utilities for visual consistency.

Output: 8 component files converted to use glass-table, glass-drop-zone, glass-badge, glass-button variants, glass-input, glass-select consistently.
</objective>

<execution_context>
@/Users/pranshu/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/pranshu/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-liquid-glass-ui-refactor/12-RESEARCH.md
@.planning/phases/12-liquid-glass-ui-refactor/12-01-SUMMARY.md
@packages/dashboard/src/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert DevicePanel and App panel system</name>
  <files>packages/dashboard/src/panels/DevicePanel.tsx, packages/dashboard/src/panels/AppPanel.tsx, packages/dashboard/src/panels/apps/AppList.tsx, packages/dashboard/src/panels/apps/AppActions.tsx, packages/dashboard/src/panels/apps/InstallDropZone.tsx</files>
  <action>
**DevicePanel.tsx:**
- Device cards already use glass-panel (keep)
- Status badges (Booted/Shutdown/etc.) → use `.glass-badge` + color-specific inline styles for border-color and color (green for booted, yellow for pending, gray for shutdown)
- Action buttons (refresh, boot, shutdown, erase):
  - Refresh → glass-button
  - Boot → glass-button-primary
  - Shutdown → glass-button
  - Erase → glass-button-destructive
- Empty state → glass-empty-state
- Device name and platform info text — ensure uses var(--color-text-primary) and var(--color-text-secondary) not hardcoded colors

**AppPanel.tsx:**
- Device select → glass-select
- App select (if any) → glass-select
- Header buttons → glass-button / glass-button-primary
- Empty state → glass-empty-state

**AppList.tsx:**
- The app list table `<table>` → add `className="glass-table"` to the table element
- Remove any inline Tailwind on `<thead>`, `<tr>`, `<th>`, `<td>` that duplicates glass-table styles (border-b, text-xs, uppercase, letter-spacing, padding)
- Keep column-specific widths, text-alignment, and truncation classes
- App name/bundleId text should use var(--color-text-primary) and var(--color-text-secondary)
- Version badges → glass-badge

**AppActions.tsx:**
- Launch button → glass-button-primary
- Terminate button → glass-button-destructive
- Uninstall button → glass-button-destructive
- Clear data button → glass-button-destructive
- Other action buttons → glass-button

**InstallDropZone.tsx:**
- Drop zone container → glass-drop-zone
- Drag-over state → add `.drag-over` class conditionally (the glass-drop-zone.drag-over CSS handles the visual change)
- Browse button inside drop zone → glass-button-primary
- Replace any inline dashed-border, padding, text-center styling with the glass-drop-zone class

IMPORTANT: Preserve all onClick handlers, disabled conditions, loading states, and conditional className logic. Only replace the static visual classes.
  </action>
  <verify>Run `npm run build --workspace=packages/dashboard`. Grep AppList.tsx for `glass-table` — should find 1 hit. Grep InstallDropZone.tsx for `glass-drop-zone` — should find 1 hit. Grep all 5 files for `bg-bg-surface/60 border border-border` — should find zero.</verify>
  <done>DevicePanel uses glass-badge and glass-button variants. AppList uses glass-table. InstallDropZone uses glass-drop-zone. All buttons use glass-button variants. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Log panel system</name>
  <files>packages/dashboard/src/panels/LogPanel.tsx, packages/dashboard/src/panels/logs/LogToolbar.tsx, packages/dashboard/src/panels/logs/LogList.tsx</files>
  <action>
**LogPanel.tsx:**
- Device select → glass-select
- Empty state → glass-empty-state
- Container already uses glass-panel (verify)

**LogToolbar.tsx:**
- Level filter buttons (verbose, debug, info, warn, error, fatal) → these are toggle buttons with color-coded active states. Use glass-button as base class, keep the conditional color classes for active state (each level has its own color). The inactive state should use glass-button appearance; active state adds the level-specific background color.
- Search input → glass-input
- Process filter input → glass-input
- Export button → glass-button
- Clear button → glass-button-destructive
- Any dropdown/select for filter mode → glass-select

**LogList.tsx:**
- The log list container — keep existing structure (virtual scrolling or overflow-auto)
- Individual log entries — these are NOT glass panels (they're content, not navigation per Apple's "glass is for navigation layer only" rule). Keep them as-is with their level-colored left border/dot.
- Ensure text colors use var(--color-text-primary) for message, var(--color-text-secondary) for timestamp/process
- Empty state → glass-empty-state

IMPORTANT: Log level buttons have complex conditional styling (each level has a different color when active). Keep the conditional logic. Just replace the BASE button appearance with glass-button, and let the active-state conditional classes override.

Example conversion:
```tsx
// Before
className={`px-2 py-1 rounded-md text-xs ${level === 'error' ? 'bg-red-500/20 text-red-400' : 'bg-bg-surface/60 text-text-muted'}`}

// After
className={`glass-button text-xs ${level === 'error' ? 'bg-red-500/20 text-red-400' : ''}`}
```
  </action>
  <verify>Run `npm run build --workspace=packages/dashboard`. Grep LogToolbar.tsx for `glass-input` — should find 1-2 hits. Grep for `glass-button` — should find multiple hits. Log entries should NOT use glass-panel (content, not navigation).</verify>
  <done>LogPanel system uses glass-select, glass-input, glass-button variants. Log entries remain non-glass content elements. Level filter buttons maintain color-coded active states. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build --workspace=packages/dashboard` passes
- All 8 files use shared glass utility classes
- Tables use glass-table, drop zones use glass-drop-zone
- Log entries are NOT glass panels (Apple rule: glass for navigation only)
- Conditional styling (active levels, disabled states) preserved
</verification>

<success_criteria>
Device, App, and Log panels are visually consistent with the Liquid Glass aesthetic. Tables, drop zones, badges, and toolbars all use the shared utility classes. Log entry content remains non-glass per Apple's Liquid Glass principles.
</success_criteria>

<output>
After completion, create `.planning/phases/12-liquid-glass-ui-refactor/12-04-SUMMARY.md`
</output>
